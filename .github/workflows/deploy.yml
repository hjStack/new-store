
name: 송파구 새로 생긴 식당을 빠르게 찾아보세요

on:
  push:
    branches: [ "main", "dev", "feature/**" ]
  pull_request:
    branches: ["main"]

# 하나의 workflows는 1개 이상의 job으로 구성
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    # steps : 특정 작업을 수행하는 가장 작은 단위
    steps:
      # 1. 깃허브 저장소에 있는 최신 코드를 로봇의 작업 공간으로 가져오기
      - name: 📥 코드 체크아웃
        uses: actions/checkout@v3

      # 2. 자바 21 세팅 (로그에서 확인한 버전 반영!)
      - name: ☕ JDK 21 설치
        uses: actions/setup-java@v3
        with:
          java-version: '21'
          distribution: 'temurin'

      # 3. application.yml 파일 생성 (보안 유지!)
      # DB 비밀번호 같은 민감한 정보는 깃허브에 올리면 안 되므로, GitHub Secrets에 숨겨두고 빌드할 때만 몰래 주입합니다.
      - name: 🛡️ application.yml 파일 만들기
        env:
          APP_YML: ${{ secrets.APPLICATION_YML }}
        run: |
          mkdir -p src/main/resources
          printf '%s' "$APP_YML" > src/main/resources/application.yml

      #      - name: application-prod.yml 생성
      #        run: |
      #          echo "${{ secrets.APPLICATION_PROD_YML }}" > src/main/resources/application-prod.yml

      # 4. 메이븐(Maven)으로 프로젝트 빌드 (.jar 파일 생성)
      - name: 프로젝트 빌드 (Maven)
        run: |
          chmod +x mvnw
          ./mvnw clean package -DskipTests

      # 5. 도커 허브(Docker Hub) 로그인
      - name: 🐳 도커 허브 로그인
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      # 6. 도커 이미지 빌드 및 도커 허브로 업로드(Push)
      - name: 🚀 도커 이미지 빌드 및 푸시
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: |
          docker build -t ${{ secrets.DOCKER_USERNAME }}/new-store-api:latest .
          docker push ${{ secrets.DOCKER_USERNAME }}/new-store-api:latest

      # 7. 설정 파일을 운영 서버로 전송
      - name: 📦 설정 파일 운영 서버로 전송
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          # nginx.prod.conf 가 운영 nginx 설정입니다. nginx/nginx.conf 는 로컬용(TLS 없음)이라
          # 서버에 보내면 안 됩니다.
          source: "docker-compose.yaml,deploy.sh,nginx/nginx.prod.conf"
          target: /home/${{ secrets.EC2_USER }}/new-store

      # 8. 운영 서버에 접속해서 무중단 배포 스크립트 실행
      - name: 🌐 운영 서버 접속 및 무중단 배포 실행
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          command_timeout: 20m
          script: |
            set -e
            cd /home/${{ secrets.EC2_USER }}/new-store
            sudo docker pull ${{ secrets.DOCKER_USERNAME }}/new-store-api:latest
            chmod +x ./deploy.sh
            ./deploy.sh
