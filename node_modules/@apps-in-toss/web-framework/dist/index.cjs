Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
//#region \0rolldown/runtime.js
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __copyProps = (to, from, except, desc) => {
	if (from && typeof from === "object" || typeof from === "function") for (var keys = __getOwnPropNames(from), i = 0, n = keys.length, key; i < n; i++) {
		key = keys[i];
		if (!__hasOwnProp.call(to, key) && key !== except) __defProp(to, key, {
			get: ((k) => from[k]).bind(null, key),
			enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable
		});
	}
	return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", {
	value: mod,
	enumerable: true
}) : target, mod));
//#endregion
let valibot = require("valibot");
valibot = __toESM(valibot, 1);
let _webview_bridge_web = require("@webview-bridge/web");
let semver = require("semver");
//#region src/webview-bridge/constants.ts
/**
* 호스트가 컨텐츠 로드 전에 동기 주입한 상수를 읽어요.
* 타입은 호스트 구현(createConstants)에서 생성된 계약 타입으로 추론돼요.
*/
function getConstant(name) {
	return window.__appsInTossConstants[name];
}
//#endregion
//#region src/webview-bridge/error.ts
const serializedErrorSchema = valibot.looseObject({
	__instanceof: valibot.literal("Error"),
	name: valibot.string(),
	message: valibot.string(),
	stack: valibot.optional(valibot.string())
});
/**
* 호스트(protocol/error.ts)가 직렬화한 에러를 복원해요.
* name/message/stack 외의 커스텀 프로퍼티도 함께 복원돼요.
* 직렬화된 에러 형태가 아니면 값을 그대로 돌려줘요.
*/
function deserializeError(serialized) {
	const parsed = valibot.safeParse(serializedErrorSchema, serialized);
	if (!parsed.success) return serialized;
	const { __instanceof: _, name, message, stack, ...props } = parsed.output;
	const error = new Error(message);
	error.name = name;
	error.stack = stack;
	Object.entries(props).forEach(([key, value]) => {
		error[key] = value;
	});
	return error;
}
/**
* async 메서드 응답이 호스트가 직렬화한 에러(SerializedError)면 복원해서 던져요.
*
* 호스트는 async 메서드에서 던져진 에러를 reject 대신 SerializedError 응답으로
* 보내요(호스트 protocol/error.ts의 withSerializedErrors). `@webview-bridge`의
* reject 직렬화가 커스텀 프로퍼티(code 등)를 버리기 때문이에요. 에러가 아니면
* 응답 값을 그대로 돌려줘요.
*/
function unwrapBridgeResponse(response) {
	const restored = deserializeError(response);
	if (restored !== response) throw restored;
	return response;
}
//#endregion
//#region src/webview-bridge/link.ts
function createLinkBridge() {
	return (0, _webview_bridge_web.linkBridge)({
		timeout: 0,
		throwOnError: true
	});
}
let instance = null;
/**
* 호스트(apps-in-toss-host)와 연결된 linkBridge 인스턴스를 반환해요.
* 첫 호출 시점에 초기화해서, import만으로는 부수효과가 없어요.
*/
function getWebViewBridge() {
	instance ??= createLinkBridge();
	return instance;
}
function assertWebViewEnvironment() {
	if (typeof window === "undefined" || window.ReactNativeWebView == null) throw new Error("apps-in-toss 웹뷰 환경이 아니에요. 토스 앱 안에서만 호출할 수 있어요.");
}
//#endregion
//#region src/webview-bridge/events.ts
const WEBVIEW_BRIDGE_EVENT_CHANNEL = "appsInTossEvent";
const envelopeSchema = valibot.object({
	subscriptionId: valibot.string(),
	kind: valibot.picklist(["event", "error"]),
	payload: valibot.optional(valibot.unknown())
});
function createSubscriptionId() {
	return Math.random().toString(36).substring(2) + Date.now().toString(36);
}
const noop = () => {};
/**
* 호스트의 이벤트 메서드를 구독해요. 반환된 함수를 호출하면 구독이 해제돼요.
* 이름·파라미터·이벤트 타입 모두 호스트 구현에서 생성된 계약 타입으로 추론돼요.
*/
function callEventMethod(name, options) {
	if (typeof window === "undefined") return noop;
	assertWebViewEnvironment();
	const bridge = getWebViewBridge();
	const subscriptionId = createSubscriptionId();
	const removeListener = bridge.addEventListener(WEBVIEW_BRIDGE_EVENT_CHANNEL, (rawEnvelope) => {
		const parsed = valibot.safeParse(envelopeSchema, rawEnvelope);
		if (!parsed.success || parsed.output.subscriptionId !== subscriptionId) return;
		const envelope = parsed.output;
		if (envelope.kind === "event") options.onEvent?.(envelope.payload);
		else options.onError?.(deserializeError(envelope.payload));
	});
	bridge.loose.__subscribeAppEvent({
		method: name,
		subscriptionId,
		params: options.params
	}).catch((error) => {
		options.onError?.(error);
	});
	return () => {
		removeListener();
		bridge.loose.__unsubscribeAppEvent({ subscriptionId }).catch(noop);
	};
}
//#endregion
//#region src/webview-bridge/methods.ts
/**
* 호스트의 async 메서드를 호출해요.
* 이름·파라미터·반환 타입 모두 호스트 구현에서 생성된 계약 타입으로 추론돼요.
* 호스트가 에러를 SerializedError 응답으로 보내면 복원해서 reject로 바꿔요.
*/
function callAsyncMethod(name, ...args) {
	assertWebViewEnvironment();
	return getWebViewBridge().loose[name](...args).then(unwrapBridgeResponse);
}
//#endregion
//#region src/apis/_bridge/createAsyncBridge/createAsyncBridge.ts
/**
* 이름 문자열로 호스트 async 브릿지를 호출하는 함수를 만들어요.
* 계약 타입의 보호를 받지 않는 탈출구예요 — 공개 API가 있으면 그쪽을 쓰세요.
*/
function createAsyncBridge(method) {
	return (...args) => {
		return getWebViewBridge().loose[method](...args).then(unwrapBridgeResponse);
	};
}
//#endregion
//#region src/apis/_bridge/createConstantBridge/createConstantBridge.ts
/**
* 이름 문자열로 호스트 주입 상수를 조회하는 함수를 만들어요.
* 계약 타입의 보호를 받지 않는 탈출구예요 — 공개 API가 있으면 그쪽을 쓰세요.
*/
function createConstantBridge(name) {
	return () => getConstant(name);
}
//#endregion
//#region src/apis/_bridge/createEventBridge/createEventBridge.ts
/**
* 이름 문자열로 호스트 이벤트 브릿지를 구독하는 함수를 만들어요.
* 계약 타입의 보호를 받지 않는 탈출구예요 — 공개 API가 있으면 그쪽을 쓰세요.
*/
function createEventBridge(method) {
	return (args) => callEventMethod(method, {
		params: args.options,
		onEvent: args.onEvent,
		onError: args.onError
	});
}
//#endregion
//#region src/apis/domains/ads/isAppsInTossAdMobLoaded/isAppsInTossAdMobLoaded.ts
/**
* Google AdMob 광고가 잘 불러와졌는지 확인해요.
* `isSupported()`로 지원 여부를 확인할 수 있어요.
*/
const isAdMobLoaded = Object.assign((options) => callAsyncMethod("getCachedStatusAppsInTossAdmob", options), { isSupported: () => getConstant("isGetCachedStatusAppsInTossAdmobSupported") });
//#endregion
//#region src/apis/domains/ads/loadAppsInTossAdMob/loadAppsInTossAdMob.ts
/**
* Google AdMob 광고를 미리 불러와서, 필요한 시점에 바로 보여줄 수 있도록 준비해요.
* `isSupported()`로 지원 여부를 확인할 수 있어요.
*/
const loadAdMob = Object.assign((params) => {
	let cancelled = false;
	callAsyncMethod("loadAppsInTossAdmob", params.options).then((data) => {
		if (!cancelled) params.onEvent({
			type: "loaded",
			data
		});
	}).catch((error) => {
		if (!cancelled) params.onError(error);
	});
	return () => {
		cancelled = true;
	};
}, { isSupported: () => getConstant("isLoadAppsInTossAdmobSupported") });
//#endregion
//#region src/apis/domains/ads/showAppsInTossAdMob/showAppsInTossAdMob.ts
/**
* 미리 불러온 Google AdMob 광고를 사용자에게 노출해요.
* `isSupported()`로 지원 여부를 확인할 수 있어요.
*/
const showAdMob = Object.assign((params) => {
	return callEventMethod("showAppsInTossAdmob", {
		params: params.options,
		onEvent: params.onEvent,
		onError: params.onError
	});
}, { isSupported: () => getConstant("isShowAppsInTossAdmobSupported") });
//#endregion
//#region src/apis/domains/ads/TossAds/banner.ts
const SDK_VERSION = "3.3.0";
function getInjectedTossAds() {
	if (typeof window === "undefined") return;
	return window.__appsInToss?.ads;
}
function getInjectedTossAdsOrThrow() {
	const ads = getInjectedTossAds();
	if (ads == null) throw new Error("[toss-ad] Failed to find injected sdk by host.");
	return ads;
}
function toError(error) {
	return error instanceof Error ? error : new Error(String(error));
}
function initialize(options) {
	try {
		getInjectedTossAdsOrThrow().initialize({
			...options,
			sdkVersion: SDK_VERSION
		});
	} catch (error) {
		options.callbacks?.onInitializationFailed?.(toError(error));
	}
}
/**
* @deprecated attach는 더 이상 권장되지 않습니다. attachBanner를 사용해주세요.
*/
function attach(adGroupId, target, options = {}) {
	try {
		getInjectedTossAdsOrThrow().attach(adGroupId, target, options);
	} catch (error) {
		options.callbacks?.onAdFailedToRender?.({
			slotId: "",
			adGroupId: adGroupId.trim(),
			adMetadata: {},
			error: {
				code: 0,
				message: toError(error).message
			}
		});
	}
}
function attachBanner(adGroupId, target, options = {}) {
	try {
		return getInjectedTossAdsOrThrow().attachBanner(adGroupId, target, options);
	} catch (error) {
		options.callbacks?.onAdFailedToRender?.({
			slotId: "",
			adGroupId: adGroupId.trim(),
			adMetadata: {},
			error: {
				code: 0,
				message: toError(error).message
			}
		});
		return { destroy() {} };
	}
}
function destroy(slotId) {
	getInjectedTossAds()?.destroy(slotId);
}
function destroyAll() {
	getInjectedTossAds()?.destroyAll();
}
function isSupported$3() {
	return getInjectedTossAds()?.initialize.isSupported() ?? false;
}
const TossAds = {
	initialize: Object.assign(initialize, { isSupported: isSupported$3 }),
	/**
	* @deprecated attach는 더 이상 권장되지 않습니다. attachBanner를 사용해주세요.
	*/
	attach: Object.assign(attach, { isSupported: isSupported$3 }),
	attachBanner: Object.assign(attachBanner, { isSupported: isSupported$3 }),
	destroy: Object.assign(destroy, { isSupported: isSupported$3 }),
	destroyAll: Object.assign(destroyAll, { isSupported: isSupported$3 })
};
//#endregion
//#region src/apis/domains/ads/loadFullScreenAd/loadFullScreenAd.ts
/**
* 전면 광고를 미리 불러와서, 보여줄 시점에 바로 노출할 수 있도록 준비해요.
* `isSupported()`로 지원 여부를 확인할 수 있어요.
*/
const loadFullScreenAd = Object.assign((params) => {
	return callEventMethod("loadFullScreenAd", {
		params: params.options,
		onEvent: params.onEvent,
		onError: params.onError
	});
}, { isSupported: () => getConstant("isLoadFullScreenAdSupported") });
//#endregion
//#region src/apis/domains/ads/showFullScreenAd/showFullScreenAd.ts
/**
* 미리 불러온 전면 광고를 사용자에게 노출해요.
* `isSupported()`로 지원 여부를 확인할 수 있어요.
*/
const showFullScreenAd = Object.assign((params) => {
	return callEventMethod("showFullScreenAd", {
		params: params.options,
		onEvent: params.onEvent,
		onError: params.onError
	});
}, { isSupported: () => getConstant("isShowFullScreenAdSupported") });
//#endregion
//#region src/apis/domains/ads/index.ts
/**
* v2 호환 별칭이에요. 도메인 멤버(`Ads.loadAdMob` 등)와 같은 인스턴스를 참조해요.
*/
const GoogleAdMob = {
	loadAppsInTossAdMob: loadAdMob,
	showAppsInTossAdMob: showAdMob,
	isAppsInTossAdMobLoaded: isAdMobLoaded
};
//#endregion
//#region src/apis/domains/analytics/internal.ts
const getReferrer = () => {
	try {
		return new URL(getConstant("schemeUri")).searchParams.get("referrer");
	} catch {
		return null;
	}
};
const getGroupId$1 = () => {
	try {
		const url = new URL(location.href);
		if (url.protocol !== "https:") throw new Error("Invalid URL");
		return {
			groupId: url.pathname,
			search: url.search.startsWith("?") ? url.search.substring(1) : url.search
		};
	} catch {
		return {
			groupId: "unknown",
			search: "unknown"
		};
	}
};
//#endregion
//#region src/apis/domains/analytics/log/log.ts
/**
* 앱에서 발생하는 이벤트를 기록해요.
*
* `params`에는 `anonymous_key`가 기본 파라미터로 자동 포함되고, `undefined`, `null` 값은
* 제외된 뒤 나머지는 string으로 정규화돼요. sandbox 환경에서는 콘솔에만 출력되고
* 전송되지 않아요. 미지원 앱 버전에서는 에러 없이 조용히 무시돼요.
*
* @deprecated 이 함수는 더 이상 사용되지 않아요. `Analytics.log`를 사용해주세요.
* @since Toss - Android 5.208.0, iOS 5.208.0
*/
const eventLog = (params) => callAsyncMethod("eventLog", params);
//#endregion
//#region src/apis/domains/analytics/_utils/extractDateFromUUIDv7.ts
const extractDateFromUUIDv7 = (uuid) => {
	const timestampHex = uuid.split("-").join("").slice(0, 12);
	return new Date(Number.parseInt(timestampHex, 16));
};
//#endregion
//#region src/apis/domains/analytics/click/click.ts
/**
* 터치나 클릭이 발생하면 분석 로그를 남겨요.
*
* @deprecated 이 함수는 더 이상 사용되지 않아요. `Analytics.click`을 사용해주세요.
*/
const click = (params = {}) => {
	const { groupId, search } = getGroupId$1();
	if (groupId === "unknown") return;
	const { log_name, ...otherParams } = params;
	return eventLog({
		log_type: "event",
		log_name: log_name ?? `${groupId}::click`,
		params: {
			...otherParams,
			search,
			referrer: getReferrer(),
			deployment_id: getConstant("deploymentId"),
			deployment_timestamp: extractDateFromUUIDv7(getConstant("deploymentId")).getTime(),
			event_type: "click"
		}
	});
};
//#endregion
//#region src/apis/domains/analytics/impression/impression.ts
/**
* 요소가 노출되면 남기는 분석 로그를 기록해요.
*
* @deprecated 이 함수는 더 이상 사용되지 않아요. `Analytics.impression`을 사용해주세요.
*/
const impression = (params = {}) => {
	const { groupId, search } = getGroupId$1();
	if (groupId === "unknown") return;
	const { log_name, ...otherParams } = params;
	return eventLog({
		log_type: "event",
		log_name: log_name ?? `${groupId}::impression`,
		params: {
			...otherParams,
			search,
			referrer: getReferrer(),
			deployment_id: getConstant("deploymentId"),
			deployment_timestamp: extractDateFromUUIDv7(getConstant("deploymentId")).getTime(),
			event_type: "impression"
		}
	});
};
//#endregion
//#region src/apis/domains/analytics/screen/screen.ts
/**
* 화면 진입 시점의 분석 로그를 남겨요.
*
* @deprecated 이 함수는 더 이상 사용되지 않아요. `Analytics.screen`을 사용해주세요.
*/
const screen = (params = {}) => {
	const { groupId, search } = getGroupId$1();
	if (groupId === "unknown") return;
	const { log_name, ...otherParams } = params;
	return eventLog({
		...params,
		log_type: "screen",
		log_name: log_name ?? `${groupId}::screen`,
		params: {
			search,
			referrer: getReferrer(),
			document_title: document.title,
			deployment_id: getConstant("deploymentId"),
			deployment_timestamp: extractDateFromUUIDv7(getConstant("deploymentId")).getTime(),
			...otherParams
		}
	});
};
//#endregion
//#region src/apis/domains/analytics/analytics.ts
const Analytics = {
	/**
	* 앱에서 발생하는 이벤트를 기록해요.
	* `params`에는 `anonymous_key`가 자동 포함되고, `undefined`는 제외된 뒤 나머지는 string으로 정규화돼요.
	* sandbox에서는 콘솔에만 출력되고, 미지원 앱 버전에서는 에러 없이 조용히 무시돼요.
	* @since Toss - Android 5.208.0, iOS 5.208.0
	*/
	log: eventLog,
	/**
	* 화면 진입 시점의 분석 로그를 남겨요.
	*/
	screen,
	/**
	* 요소가 노출되면 남기는 분석 로그를 기록해요.
	*/
	impression,
	/**
	* 터치나 클릭이 발생하면 분석 로그를 남겨요.
	*/
	click
};
//#endregion
//#region src/apis/domains/permissions/PermissionError.ts
/**
* 권한 에러를 나타내는 클래스예요. 공통된 권한에러를 처리할 때 사용해요.
* 에러가 발생했을 때 `error instanceof PermissionError`를 통해 확인할 수 있어요.
*/
var PermissionError = class extends Error {
	constructor({ methodName, message }) {
		super();
		this.name = `${methodName} permission error`;
		this.message = message;
	}
};
//#endregion
//#region src/apis/domains/permissions/GetClipboardTextPermissionError.ts
/**
* 클립보드 읽기 권한이 거부되었을 때 발생하는 에러예요.
* 에러가 발생했을 때 `error instanceof GetClipboardTextPermissionError`를 통해 확인할 수 있어요.
*/
var GetClipboardTextPermissionError = class extends PermissionError {
	constructor() {
		super({
			methodName: "getClipboardText",
			message: "클립보드 읽기 권한이 거부되었어요."
		});
	}
};
//#endregion
//#region src/apis/domains/permissions/withPermission.ts
function withPermission(fn, permission, errorClass) {
	const wrapped = async (...args) => {
		if (await callAsyncMethod("requestPermission", permission) === "denied") throw new errorClass();
		return fn(...args);
	};
	return Object.assign(wrapped, {
		getPermission: () => callAsyncMethod("getPermission", permission),
		openPermissionDialog: () => callAsyncMethod("openPermissionDialog", permission)
	});
}
//#endregion
//#region src/apis/domains/clipboard/getText/getText.ts
/**
* 클립보드에 저장된 텍스트를 가져와요. 텍스트가 없으면 빈 문자열을 반환해요.
* 권한이 없으면 `GetClipboardTextPermissionError`가 발생해요.
*
* @deprecated 이 함수는 더 이상 사용되지 않아요. `Clipboard.getText`를 사용해주세요.
*/
const getClipboardText = withPermission(() => callAsyncMethod("getClipboardText"), {
	name: "clipboard",
	access: "read"
}, GetClipboardTextPermissionError);
//#endregion
//#region src/apis/domains/permissions/SetClipboardTextPermissionError.ts
/**
* 클립보드 쓰기 권한이 거부되었을 때 발생하는 에러예요.
* 에러가 발생했을 때 `error instanceof SetClipboardTextPermissionError`를 통해 확인할 수 있어요.
*/
var SetClipboardTextPermissionError = class extends PermissionError {
	constructor() {
		super({
			methodName: "setClipboardText",
			message: "클립보드 쓰기 권한이 거부되었어요."
		});
	}
};
//#endregion
//#region src/apis/domains/clipboard/setText/setText.ts
/**
* 텍스트를 클립보드에 복사해요.
* 권한이 없으면 `SetClipboardTextPermissionError`가 발생해요.
*
* @deprecated 이 함수는 더 이상 사용되지 않아요. `Clipboard.setText`를 사용해주세요.
*/
const setClipboardText = withPermission((text) => callAsyncMethod("setClipboardText", { text }), {
	name: "clipboard",
	access: "write"
}, SetClipboardTextPermissionError);
//#endregion
//#region src/apis/domains/clipboard/clipboard.ts
const Clipboard = {
	/**
	* 클립보드에 저장된 텍스트를 가져와요. 텍스트가 없으면 빈 문자열을 반환해요.
	* 권한이 없으면 `GetClipboardTextPermissionError`가 발생해요.
	*/
	getText: getClipboardText,
	/**
	* 텍스트를 클립보드에 복사해요.
	* 권한이 없으면 `SetClipboardTextPermissionError`가 발생해요.
	*/
	setText: setClipboardText
};
//#endregion
//#region src/apis/domains/environment/isMinVersionSupported/isMinVersionSupported.ts
/**
* 현재 토스 앱 버전이 플랫폼별 최소 지원 버전 이상인지 확인해요.
*
* - 샌드박스 환경에서는 버전과 무관하게 항상 `true`를 반환해요.
* - 현재 플랫폼의 최소 버전이 지정되지 않았으면 `false`를 반환해요.
* - `'always'`는 항상 지원, `'never'`는 항상 미지원으로 처리해요.
*/
function isMinVersionSupported(minVersions) {
	if (getConstant("operationalEnvironment") === "sandbox") return true;
	const currentVersion = getConstant("tossAppVersion");
	const minVersion = getConstant("platformOS") === "ios" ? minVersions.ios : minVersions.android;
	if (minVersion === void 0) return false;
	if (minVersion === "always") return true;
	if (minVersion === "never") return false;
	return (0, semver.gte)((0, semver.coerce)(currentVersion) ?? currentVersion, minVersion);
}
//#endregion
//#region src/utils/withUnsupportedThrow.ts
const UNSUPPORTED_APP_VERSION = "UNSUPPORTED_APP_VERSION";
const UNSUPPORTED_OS_VERSION = "UNSUPPORTED_OS_VERSION";
/** 구 SDK(MIN_VERSION)와 동일한 형식의 `MIN_TOSS_APP_VERSION` 기반으로 안내 메시지를 만들어요. */
function formatUnsupportedAppVersionMessage({ android, ios }) {
	if (android == null && ios == null) return "현재 토스앱 버전에서 사용할 수 없는 기능이에요. 토스앱을 최신 버전으로 업데이트해 주세요.";
	if (android != null && ios != null) return android === ios ? `토스앱 ${android} 이상에서 사용할 수 있어요.` : `토스앱 Android ${android}, iOS ${ios} 이상에서 사용할 수 있어요.`;
	return `${android != null ? "Android" : "iOS"} 토스앱 ${android ?? ios} 이상에서만 사용할 수 있어요.`;
}
function createUnsupportedAppVersionError(minVersion) {
	const error = new Error(formatUnsupportedAppVersionMessage(minVersion));
	error.name = UNSUPPORTED_APP_VERSION;
	error.code = UNSUPPORTED_APP_VERSION;
	return error;
}
/** `MIN_OS_VERSION` 기반으로 OS 업데이트 안내 메시지를 만들어요. 키가 없는 플랫폼은 지원하지 않는다는 뜻이에요. */
function formatUnsupportedOSVersionMessage({ android, ios }) {
	if (android == null && ios == null) return "현재 기기의 OS 버전에서 사용할 수 없는 기능이에요. OS를 최신 버전으로 업데이트해 주세요.";
	if (android != null && ios != null) return `Android ${android}, iOS ${ios} 이상에서만 사용할 수 있어요. 기기의 OS를 업데이트해 주세요.`;
	return `${android != null ? "Android" : "iOS"} ${android ?? ios} 이상에서만 사용할 수 있어요.`;
}
function createUnsupportedOSVersionError(minOSVersion) {
	const error = new Error(formatUnsupportedOSVersionMessage(minOSVersion));
	error.name = UNSUPPORTED_OS_VERSION;
	error.code = UNSUPPORTED_OS_VERSION;
	return error;
}
/** 웹 로컬(`tossAppVersion`)로 토스앱 버전 충족 여부를 확정해요. 키가 없는 플랫폼은 미지원으로 판정해요. */
function isTossAppVersionSupported(minVersion) {
	return isMinVersionSupported(minVersion);
}
/**
* 미지원 환경이면 부족한 쪽의 에러를 던져요.
*
* `MIN_OS_VERSION`이 없는 표면은 통합 판정(`isSupported`) 하나로 `UNSUPPORTED_APP_VERSION`을 던지고,
* `MIN_OS_VERSION`이 있는 표면은 실패 원인을 나눠서 판정해요.
* OS 버전은 웹뷰에 주입되지 않아서, 앱 버전이 충족된 상태의 통합 판정 실패를 OS 부족으로 확정해요.
*/
function assertSupported(fn, isSupported) {
	const { MIN_OS_VERSION, MIN_TOSS_APP_VERSION } = fn;
	if (MIN_OS_VERSION == null) {
		if (!isSupported()) throw createUnsupportedAppVersionError(MIN_TOSS_APP_VERSION ?? {});
		return;
	}
	if (MIN_OS_VERSION[getConstant("platformOS")] == null) throw createUnsupportedOSVersionError(MIN_OS_VERSION);
	if (MIN_TOSS_APP_VERSION != null && !isTossAppVersionSupported(MIN_TOSS_APP_VERSION)) throw createUnsupportedAppVersionError(MIN_TOSS_APP_VERSION);
	if (!isSupported()) throw createUnsupportedOSVersionError(MIN_OS_VERSION);
}
/**
* 도메인 멤버를 감싸, 미지원 환경이면 부족한 쪽을 알려주는 에러를 throw해요.
* OS 버전이 부족하면 `UNSUPPORTED_OS_VERSION`, 토스앱 버전이 부족하면 `UNSUPPORTED_APP_VERSION`이고,
* 에러 메시지는 `fn.MIN_OS_VERSION`/`fn.MIN_TOSS_APP_VERSION`으로 만들어요.
*
* 지원 여부 판정은 두 가지예요.
* - `flag`를 넘기면 호스트가 주입한 `is*Supported` 상수를 SSoT로 써요.
* - `flag`가 없으면 구 SDK처럼 flat 함수에 붙은 `fn.isSupported`를 그대로 써요.
*/
function withUnsupportedThrow(fn, flag) {
	const isSupported = flag != null ? () => getConstant(flag) : () => fn.isSupported?.() !== false;
	return Object.assign(((...args) => {
		assertSupported(fn, isSupported);
		return fn(...args);
	}), fn, { isSupported });
}
//#endregion
//#region src/apis/domains/device/getAlbumItems/getAlbumItems.ts
/**
* 앨범에서 사진·동영상을 선택해 가져와요. 취소하면 빈 배열을 반환하고, `types`를 안 넘기면 사진만 선택돼요.
* `isSupported()`로 지원 여부를 확인할 수 있어요.
* @since Toss - Android 5.261.0, iOS 5.261.0
*
* @deprecated 이 함수는 더 이상 사용되지 않아요. `Device.getAlbumItems`를 사용해주세요.
*/
const fetchAlbumItems = Object.assign((options) => callAsyncMethod("fetchAlbumItems", options ?? {}), { isSupported: () => getConstant("isFetchAlbumItemsSupported") });
//#endregion
//#region src/apis/domains/permissions/FetchContactsPermissionError.ts
/**
* 연락처 권한이 거부되었을 때 발생하는 에러예요.
* 에러가 발생했을 때 `error instanceof FetchContactsPermissionError`를 통해 확인할 수 있어요.
*/
var FetchContactsPermissionError = class extends PermissionError {
	constructor() {
		super({
			methodName: "fetchContacts",
			message: "연락처 권한이 거부되었어요."
		});
	}
};
//#endregion
//#region src/apis/domains/device/getContacts/getContacts.ts
/**
* 연락처 목록을 페이지 단위로 가져와요. `offset`은 첫 호출에 `0`, 이후 `nextOffset`을 사용해요.
* 권한이 없으면 `FetchContactsPermissionError`가 발생해요.
*
* @deprecated 이 함수는 더 이상 사용되지 않아요. `Device.getContacts`를 사용해주세요.
*/
const fetchContacts = withPermission((options) => callAsyncMethod("fetchContacts", options), {
	name: "contacts",
	access: "read"
}, FetchContactsPermissionError);
//#endregion
//#region src/apis/domains/permissions/GetCurrentLocationPermissionError.ts
/**
* 위치 권한이 거부되었을 때 발생하는 에러예요.
* 에러가 발생했을 때 `error instanceof GetCurrentLocationPermissionError`를 통해 확인할 수 있어요.
*/
var GetCurrentLocationPermissionError = class extends PermissionError {
	constructor() {
		super({
			methodName: "getCurrentLocation",
			message: "위치 권한이 거부되었어요."
		});
	}
};
//#endregion
//#region src/apis/domains/device/getLocation/getLocation.ts
let Accuracy = /* @__PURE__ */ function(Accuracy) {
	Accuracy[Accuracy["Lowest"] = 1] = "Lowest";
	Accuracy[Accuracy["Low"] = 2] = "Low";
	Accuracy[Accuracy["Balanced"] = 3] = "Balanced";
	Accuracy[Accuracy["High"] = 4] = "High";
	Accuracy[Accuracy["Highest"] = 5] = "Highest";
	Accuracy[Accuracy["BestForNavigation"] = 6] = "BestForNavigation";
	return Accuracy;
}({});
/**
* 디바이스의 현재 위치를 한 번 조회해요. 지속 감지가 필요하면 `startUpdateLocation`(`Device.subscribeLocation`)을 사용하세요.
* 권한이 없으면 `GetCurrentLocationPermissionError`가 발생해요.
*
* @deprecated 이 함수는 더 이상 사용되지 않아요. `Device.getLocation`을 사용해주세요.
*/
const getCurrentLocation = withPermission((options) => callAsyncMethod("getCurrentLocation", options), {
	name: "geolocation",
	access: "access"
}, GetCurrentLocationPermissionError);
//#endregion
//#region src/apis/domains/permissions/FetchAlbumPhotosPermissionError.ts
/**
* 사진첩 권한이 거부되었을 때 발생하는 에러예요.
* 에러가 발생했을 때 `error instanceof FetchAlbumPhotosPermissionError`를 통해 확인할 수 있어요.
*/
var FetchAlbumPhotosPermissionError = class extends PermissionError {
	constructor() {
		super({
			methodName: "fetchAlbumPhotos",
			message: "사진첩 권한이 거부되었어요."
		});
	}
};
//#endregion
//#region src/apis/domains/device/getPhotos/getPhotos.ts
/**
* 앨범에서 사진 목록을 불러와요.
* 기본값은 `maxCount=10`, `maxWidth=1024`, `base64=false`예요. 권한이 없으면 `FetchAlbumPhotosPermissionError`가 발생해요.
*
* @deprecated 이 함수는 더 이상 사용되지 않아요. `Device.getPhotos`를 사용해주세요.
*/
const fetchAlbumPhotos = withPermission((options) => callAsyncMethod("fetchAlbumPhotos", options ?? {}), {
	name: "photos",
	access: "read"
}, FetchAlbumPhotosPermissionError);
//#endregion
//#region src/apis/domains/device/locale/locale.ts
/**
* 사용자 로케일 정보를 가져와요. 값을 못 가져오면 기본값은 `ko-KR`이에요.
*
* @deprecated 이 함수는 더 이상 사용되지 않아요. `Device.locale`을 사용해주세요.
*/
function getLocale() {
	return getConstant("locale");
}
//#endregion
//#region src/apis/domains/permissions/OpenCameraPermissionError.ts
/**
* 카메라 권한이 거부되었을 때 발생하는 에러예요.
* 에러가 발생했을 때 `error instanceof OpenCameraPermissionError`를 통해 확인할 수 있어요.
*/
var OpenCameraPermissionError = class extends PermissionError {
	constructor() {
		super({
			methodName: "openCamera",
			message: "카메라 권한이 거부되었어요."
		});
	}
};
//#endregion
//#region src/apis/domains/device/openCamera/openCamera.ts
/**
* 카메라를 실행해 촬영한 이미지를 `{ id, dataUri }`로 반환해요.
* 기본값은 `base64=false`, `maxWidth=1024`예요. 권한이 없으면 `OpenCameraPermissionError`가 발생해요.
*
* @deprecated 이 함수는 더 이상 사용되지 않아요. `Device.openCamera`를 사용해주세요.
*/
const openCamera = withPermission((options) => callAsyncMethod("openCamera", options ?? {}), {
	name: "camera",
	access: "access"
}, OpenCameraPermissionError);
//#endregion
//#region src/apis/domains/device/openURL/openURL.ts
/**
* 지정한 URL을 기기의 기본 브라우저나 관련 앱에서 열어요.
*
* @deprecated 이 함수는 더 이상 사용되지 않아요. `Device.openURL`을 사용해주세요.
*/
const openURL = (url) => callAsyncMethod("openURL", { url });
//#endregion
//#region src/apis/domains/device/os/os.ts
/**
* 현재 플랫폼(`ios`/`android`) 정보를 가져와요.
*
* @deprecated 이 함수는 더 이상 사용되지 않아요. `Device.os`를 사용해주세요.
*/
function getPlatformOS() {
	return getConstant("platformOS");
}
//#endregion
//#region src/apis/domains/permissions/StartUpdateLocationPermissionError.ts
/**
* 위치 업데이트 권한이 거부되었을 때 발생하는 에러예요.
* `GetCurrentLocationPermissionError`와 동일하며,
* `error instanceof StartUpdateLocationPermissionError`로 확인할 수 있어요.
*/
const StartUpdateLocationPermissionError = GetCurrentLocationPermissionError;
//#endregion
//#region src/apis/domains/device/subscribeLocation/subscribeLocation.ts
/**
* 위치 변경을 계속 감지하고 콜백을 실행해요. 반환되는 cleanup 함수로 구독을 해제해야 해요.
* `timeInterval`은 시스템 영향으로 지정값보다 길어질 수 있어요.
* 권한이 없으면 `onError`에 `StartUpdateLocationPermissionError`가 전달돼요.
*
* @deprecated 이 함수는 더 이상 사용되지 않아요. `Device.subscribeLocation`을 사용해주세요.
*/
const startUpdateLocation = Object.assign((eventParams) => {
	let unsubscribe = null;
	let cancelled = false;
	callAsyncMethod("requestPermission", {
		name: "geolocation",
		access: "access"
	}).then((status) => {
		if (cancelled) return;
		if (status === "denied") {
			eventParams.onError(new StartUpdateLocationPermissionError());
			return;
		}
		unsubscribe = callEventMethod("startUpdateLocation", {
			params: eventParams.options,
			onEvent: eventParams.onEvent,
			onError: (error) => {
				const locationError = new StartUpdateLocationPermissionError();
				if (error instanceof Error && error.name === locationError.name) return eventParams.onError(locationError);
				return eventParams.onError(error);
			}
		});
	}).catch((error) => {
		eventParams.onError(error);
	});
	return () => {
		cancelled = true;
		unsubscribe?.();
		unsubscribe = null;
	};
}, {
	getPermission: () => callAsyncMethod("getPermission", {
		name: "geolocation",
		access: "access"
	}),
	openPermissionDialog: () => callAsyncMethod("openPermissionDialog", {
		name: "geolocation",
		access: "access"
	})
});
//#endregion
//#region src/apis/domains/device/triggerHaptic/triggerHaptic.ts
/**
* 디바이스에 햅틱 진동을 일으켜요. `type`이 필수예요.
*
* @deprecated 이 함수는 더 이상 사용되지 않아요. `Device.triggerHaptic`을 사용해주세요.
*/
const generateHapticFeedback = (options) => callAsyncMethod("generateHapticFeedback", options);
//#endregion
//#region src/apis/domains/device/device.ts
const Device = {
	/**
	* 사용자 로케일 정보예요. 값을 못 가져오면 기본값은 `ko-KR`이에요.
	*/
	get locale() {
		return getLocale();
	},
	/**
	* 현재 플랫폼(`ios`/`android`) 정보예요.
	*/
	get os() {
		return getPlatformOS();
	},
	/**
	* 앨범에서 사진 목록을 불러와요.
	* 기본값은 `maxCount=10`, `maxWidth=1024`, `base64=false`예요. 권한이 없으면 `FetchAlbumPhotosPermissionError`가 발생해요.
	*/
	getPhotos: fetchAlbumPhotos,
	/**
	* 연락처 목록을 페이지 단위로 가져와요. `offset`은 첫 호출에 `0`, 이후 `nextOffset`을 사용해요.
	* 권한이 없으면 `FetchContactsPermissionError`가 발생해요.
	*/
	getContacts: fetchContacts,
	/**
	* 디바이스의 현재 위치를 한 번 조회해요. 지속 감지가 필요하면 `subscribeLocation`을 사용하세요.
	* 권한이 없으면 `GetCurrentLocationPermissionError`가 발생해요.
	*/
	getLocation: getCurrentLocation,
	/**
	* 카메라를 실행해 촬영한 이미지를 `{ id, dataUri }`로 반환해요.
	* 기본값은 `base64=false`, `maxWidth=1024`예요. 권한이 없으면 `OpenCameraPermissionError`가 발생해요.
	*/
	openCamera,
	/**
	* 앨범에서 사진·동영상을 선택해 가져와요. 취소하면 빈 배열을 반환하고, `types`를 안 넘기면 사진만 선택돼요.
	* @since Toss - Android 5.261.0, iOS 5.261.0
	*/
	getAlbumItems: withUnsupportedThrow(fetchAlbumItems, "isFetchAlbumItemsSupported"),
	/**
	* 디바이스에 햅틱 진동을 일으켜요. `type`이 필수예요.
	*/
	triggerHaptic: generateHapticFeedback,
	/**
	* 위치 변경을 계속 감지하고 콜백을 실행해요. 반환되는 cleanup 함수로 구독을 해제해야 해요.
	* `timeInterval`은 시스템 영향으로 지정값보다 길어질 수 있어요.
	* 권한이 없으면 `StartUpdateLocationPermissionError`가 발생해요.
	*/
	subscribeLocation: startUpdateLocation,
	/**
	* 지정한 URL을 기기의 기본 브라우저나 관련 앱에서 열어요.
	*/
	openURL
};
//#endregion
//#region src/apis/domains/environment/deploymentId/deploymentId.ts
/**
* 배포 정보 함수형 API 묶음이에요.
*/
const env = { 
/**
* 현재 배포 ID를 가져와요. 개발 환경에서는 `'local'`이에요.
*
* @deprecated 이 함수는 더 이상 사용되지 않아요. `Environment.deploymentId`를 사용해주세요.
*/
getDeploymentId: () => getConstant("deploymentId") };
//#endregion
//#region src/apis/domains/environment/deviceId/deviceId.ts
/**
* 기기 고유 식별자를 가져와요.
*
* @deprecated 이 함수는 더 이상 사용되지 않아요. `Environment.deviceId`를 사용해주세요.
*/
function getDeviceId() {
	return getConstant("deviceId");
}
//#endregion
//#region src/apis/domains/environment/environment/environment.ts
/**
* 운영 환경(`toss`/`sandbox`) 정보를 가져와요.
*
* @deprecated 이 함수는 더 이상 사용되지 않아요. `Environment.environment`를 사용해주세요.
*/
function getOperationalEnvironment() {
	return getConstant("operationalEnvironment");
}
//#endregion
//#region src/apis/domains/environment/getNetworkStatus/getNetworkStatus.ts
/**
* 디바이스의 현재 네트워크 연결 상태를 가져와요.
* `OFFLINE` | `WIFI` | `2G`–`5G` | `WWAN`(iOS) | `UNKNOWN`(Android) 중 하나예요.
*
* @deprecated 이 함수는 더 이상 사용되지 않아요. `Environment.getNetworkStatus`를 사용해주세요.
*/
const getNetworkStatus = () => {
	return callAsyncMethod("getNetworkStatus");
};
/**
* 토스 앱 서버의 현재 시간을 Unix timestamp(ms)로 가져와요. 디바이스 시계가 아니에요.
* `isSupported()`로 지원 여부를 확인할 수 있어요.
* @since Toss - Android 5.245.0, iOS 5.245.0
*
* @deprecated 이 함수는 더 이상 사용되지 않아요. `Environment.getServerTime`을 사용해주세요.
*/
const getServerTime = Object.assign(async () => {
	return (await callAsyncMethod("getServerTime")).serverTime;
}, {
	MIN_TOSS_APP_VERSION: {
		android: "5.245.0",
		ios: "5.245.0"
	},
	isSupported: () => getConstant("isGetServerTimeSupported")
});
//#endregion
//#region src/apis/domains/environment/groupId/groupId.ts
/**
* 앱인토스 그룹 ID를 가져와요.
*
* @deprecated 이 함수는 더 이상 사용되지 않아요. `Environment.groupId`를 사용해주세요.
*/
function getGroupId() {
	return getConstant("groupId");
}
//#endregion
//#region src/apis/domains/environment/initialURL/initialURL.ts
/**
* 앱에 처음 진입할 때 사용한 스킴 URL을 가져와요. 이후 페이지 이동은 반영되지 않아요.
*
* @deprecated 이 함수는 더 이상 사용되지 않아요. `Environment.initialURL`을 사용해주세요.
*/
function getSchemeUri() {
	return getConstant("schemeUri");
}
//#endregion
//#region src/apis/domains/environment/tossAppVersion/tossAppVersion.ts
/**
* 토스 앱 버전 정보를 가져와요.
*
* @deprecated 이 함수는 더 이상 사용되지 않아요. `Environment.tossAppVersion`을 사용해주세요.
*/
function getTossAppVersion() {
	return getConstant("tossAppVersion");
}
//#endregion
//#region src/apis/domains/environment/environment.ts
const Environment = {
	/**
	* 기기 고유 식별자예요.
	*/
	get deviceId() {
		return getDeviceId();
	},
	/**
	* 앱인토스 그룹 ID예요.
	*/
	get groupId() {
		return getGroupId();
	},
	/**
	* 운영 환경(`toss`/`sandbox`) 정보예요.
	*/
	get environment() {
		return getOperationalEnvironment();
	},
	/**
	* 토스 앱 버전 정보예요.
	*/
	get tossAppVersion() {
		return getTossAppVersion();
	},
	/**
	* 현재 배포 ID예요. 개발 환경에서는 `'local'`이에요.
	*/
	get deploymentId() {
		return env.getDeploymentId();
	},
	/**
	* 앱에 처음 진입할 때 사용한 스킴 URL이에요. 이후 페이지 이동은 반영되지 않아요.
	*/
	get initialURL() {
		return getSchemeUri();
	},
	/**
	* 디바이스의 현재 네트워크 연결 상태를 가져와요.
	* `OFFLINE` | `WIFI` | `2G`–`5G` | `WWAN`(iOS) | `UNKNOWN`(Android) 중 하나예요.
	*/
	getNetworkStatus,
	/**
	* 토스 앱 서버의 현재 시간을 Unix timestamp(ms)로 가져와요. 디바이스 시계가 아니에요.
	* @since Toss - Android 5.245.0, iOS 5.245.0
	*/
	getServerTime: withUnsupportedThrow(getServerTime, "isGetServerTimeSupported")
};
//#endregion
//#region src/apis/domains/environment/getAppsInTossGlobals/getAppsInTossGlobals.ts
/**
* 호스트가 주입한 앱인토스 전역 값을 가져와요. 배포 ID와 브랜드 정보를 담고 있어요.
*/
function getAppsInTossGlobals() {
	return getConstant("appsInTossGlobals");
}
/**
* Base64로 인코딩된 PDF 데이터를 네이티브 PDF 뷰어로 열어요. 뷰어를 닫으면 `'CLOSE'`를 반환해요.
* `openPDFViewer.isSupported()`로 지원 여부를 확인할 수 있어요.
* @since Toss - Android 5.261.0, iOS 5.261.0
*
* @deprecated 이 함수는 더 이상 사용되지 않아요. `File.openPDFViewer`를 사용해주세요.
*/
const openPDFViewer = Object.assign((params) => callAsyncMethod("openPDFViewer", params), {
	MIN_TOSS_APP_VERSION: {
		android: "5.261.0",
		ios: "5.261.0"
	},
	isSupported: () => getConstant("isOpenPDFViewerSupported")
});
//#endregion
//#region src/apis/domains/file/saveBase64/saveBase64.ts
const MIN_TOSS_APP_VERSION$16 = {
	android: "5.218.0",
	ios: "5.216.0"
};
/**
* Base64로 인코딩된 데이터를 지정한 파일 이름과 MIME 타입으로 기기에 저장해요.
* `fileName`에는 확장자를 포함해야 해요.
* 앱 버전이 최소 지원 버전보다 낮으면 경고 로그만 남기고 아무 동작도 하지 않아요.
* @since Toss - Android 5.218.0, iOS 5.216.0
*
* @deprecated 이 함수는 더 이상 사용되지 않아요. `File.saveBase64`를 사용해주세요.
*/
const saveBase64Data = Object.assign(async (params) => {
	if (!saveBase64Data.isSupported()) {
		console.warn("saveBase64Data is not supported in this app version");
		return;
	}
	await callAsyncMethod("saveBase64Data", params);
}, {
	MIN_TOSS_APP_VERSION: MIN_TOSS_APP_VERSION$16,
	isSupported: () => isMinVersionSupported(MIN_TOSS_APP_VERSION$16)
});
//#endregion
//#region src/apis/domains/file/file.ts
const File = {
	/**
	* Base64로 인코딩된 데이터를 지정한 파일 이름과 MIME 타입으로 기기에 저장해요.
	* `fileName`에는 확장자를 포함해야 해요.
	* @since Toss - Android 5.218.0, iOS 5.216.0
	*/
	saveBase64: withUnsupportedThrow(saveBase64Data),
	/**
	* Base64로 인코딩된 PDF 데이터를 네이티브 PDF 뷰어로 열어요. 뷰어를 닫으면 `'CLOSE'`를 반환해요.
	* @since Toss - Android 5.261.0, iOS 5.261.0
	*/
	openPDFViewer: withUnsupportedThrow(openPDFViewer, "isOpenPDFViewerSupported")
};
//#endregion
//#region src/apis/domains/game/getUserProfile/getUserProfile.ts
const MIN_TOSS_APP_VERSION$15 = {
	android: "5.221.0",
	ios: "5.221.0"
};
/**
* 토스게임센터 프로필 정보를 가져와요.
* 프로필이 없으면 `statusCode: 'PROFILE_NOT_FOUND'`를 반환해요.
* 앱 버전이 최소 지원 버전보다 낮으면 아무 동작도 하지 않고 `undefined`를 반환해요.
* @since Toss - Android 5.221.0, iOS 5.221.0
*
* @deprecated 이 함수는 더 이상 사용되지 않아요. `Game.getUserProfile`을 사용해주세요.
*/
const getGameCenterGameProfile = Object.assign(async () => {
	if (!getGameCenterGameProfile.isSupported()) return;
	return callAsyncMethod("getGameCenterGameProfile");
}, {
	MIN_TOSS_APP_VERSION: MIN_TOSS_APP_VERSION$15,
	isSupported: () => isMinVersionSupported(MIN_TOSS_APP_VERSION$15)
});
//#endregion
//#region src/apis/domains/game/openLeaderboard/openLeaderboard.ts
const MIN_TOSS_APP_VERSION$14 = {
	android: "5.221.0",
	ios: "5.221.0"
};
/**
* 게임센터 리더보드 웹뷰를 열어요.
* 앱 버전이 최소 지원 버전보다 낮으면 아무 동작도 하지 않고 `undefined`를 반환해요.
* @since Toss - Android 5.221.0, iOS 5.221.0
*
* @deprecated 이 함수는 더 이상 사용되지 않아요. `Game.openLeaderboard`를 사용해주세요.
*/
const openGameCenterLeaderboard = Object.assign(async () => {
	if (!openGameCenterLeaderboard.isSupported()) return;
	return callAsyncMethod("openGameCenterLeaderboard");
}, {
	MIN_TOSS_APP_VERSION: MIN_TOSS_APP_VERSION$14,
	isSupported: () => isMinVersionSupported(MIN_TOSS_APP_VERSION$14)
});
//#endregion
//#region src/apis/domains/game/setLeaderboardScore/setLeaderboardScore.ts
const MIN_TOSS_APP_VERSION$13 = {
	android: "5.221.0",
	ios: "5.221.0"
};
/**
* 사용자의 게임 점수를 토스게임센터 리더보드에 제출해요.
* `score`는 실수형 숫자의 문자열이어야 해요. 예: `"123.45"`
* 앱 버전이 최소 지원 버전보다 낮으면 아무 동작도 하지 않고 `undefined`를 반환해요.
* @since Toss - Android 5.221.0, iOS 5.221.0
*
* @deprecated 이 함수는 더 이상 사용되지 않아요. `Game.setLeaderboardScore`를 사용해주세요.
*/
const submitGameCenterLeaderBoardScore = Object.assign(async (params) => {
	if (!submitGameCenterLeaderBoardScore.isSupported()) return;
	return callAsyncMethod("submitGameCenterLeaderBoardScore", params);
}, {
	MIN_TOSS_APP_VERSION: MIN_TOSS_APP_VERSION$13,
	isSupported: () => isMinVersionSupported(MIN_TOSS_APP_VERSION$13)
});
//#endregion
//#region src/apis/domains/game/subscribeInfoOverlayHidden/subscribeInfoOverlayHidden.ts
/**
* 게임 정보 오버레이가 완전히 사라진 시점을 구독해요.
* 반환되는 구독 해제 함수는 더 이상 이벤트가 필요하지 않을 때 호출하세요.
*/
const subscribeInfoOverlayHidden = (eventParams) => callEventMethod("gameInfoOverlayHidden", {
	onEvent: eventParams.onEvent,
	onError: eventParams.onError
});
//#endregion
//#region src/apis/domains/game/game.ts
const Game = {
	/**
	* 게임센터 리더보드 웹뷰를 열어요.
	* @since Toss - Android 5.221.0, iOS 5.221.0
	*/
	openLeaderboard: withUnsupportedThrow(openGameCenterLeaderboard),
	/**
	* 사용자의 게임 점수를 토스게임센터 리더보드에 제출해요.
	* `score`는 실수형 숫자의 문자열이어야 해요. 예: `"123.45"`
	* @since Toss - Android 5.221.0, iOS 5.221.0
	*/
	setLeaderboardScore: withUnsupportedThrow(submitGameCenterLeaderBoardScore),
	/**
	* 토스게임센터 프로필 정보를 가져와요.
	* 프로필이 없으면 `statusCode: 'PROFILE_NOT_FOUND'`를 반환해요.
	* @since Toss - Android 5.221.0, iOS 5.221.0
	*/
	getUserProfile: withUnsupportedThrow(getGameCenterGameProfile),
	/**
	* 게임 정보 오버레이가 완전히 사라진 시점을 구독해요.
	* 반환되는 구독 해제 함수는 더 이상 이벤트가 필요하지 않을 때 호출하세요.
	*/
	subscribeInfoOverlayHidden
};
//#endregion
//#region src/apis/domains/iap/_utils/minVersions.ts
/**
* IAP 멤버별 최소 지원 토스앱 버전이에요.
*/
const IAP_MIN_VERSION = {
	android: "5.219.0",
	ios: "5.219.0"
};
const IAP_PENDING_ORDERS_MIN_VERSION = {
	android: "5.234.0",
	ios: "5.231.0"
};
const IAP_COMPLETED_OR_REFUNDED_ORDERS_MIN_VERSION = {
	android: "5.231.0",
	ios: "5.231.0"
};
const IAP_COMPLETE_PRODUCT_GRANT_MIN_VERSION = {
	android: "5.233.0",
	ios: "5.233.0"
};
const IAP_SUBSCRIPTION_INFO_MIN_VERSION = {
	android: "5.253.0",
	ios: "5.250.0"
};
const IAP_SUBSCRIPTION_PURCHASE_MIN_VERSION = {
	android: "5.248.0",
	ios: "5.249.0"
};
//#endregion
//#region src/apis/domains/iap/completeProductGrant/completeProductGrant.ts
/**
* 상품 지급 완료를 앱에 알려요. `{ params: { orderId } }` 형태이며, 지급 완료 여부(boolean)를 반환해요.
* @since Toss - Android 5.233.0, iOS 5.233.0
*
* @deprecated 이 함수는 더 이상 사용되지 않아요. `IAP.completeProductGrant`을 사용해주세요.
*/
const completeProductGrant = Object.assign((args) => callAsyncMethod("completeProductGrant", args.params), {
	MIN_TOSS_APP_VERSION: IAP_COMPLETE_PRODUCT_GRANT_MIN_VERSION,
	isSupported: () => isMinVersionSupported(IAP_COMPLETE_PRODUCT_GRANT_MIN_VERSION)
});
//#endregion
//#region src/apis/domains/iap/createOneTimePurchaseOrder/createOneTimePurchaseOrder.ts
/**
* 단건 인앱결제 주문서 페이지로 이동해요.
* 반환되는 cleanup 함수는 결제 흐름이 끝나면 반드시 호출해야 해요.
* 상품 식별자는 `sku`를 사용하세요. (`productId`는 deprecated)
* @since Toss - Android 5.219.0, iOS 5.219.0
*
* @deprecated 이 함수는 더 이상 사용되지 않아요. `IAP.createOneTimePurchaseOrder`을 사용해주세요.
*/
const createOneTimePurchaseOrder = Object.assign((params) => {
	const noop = () => {};
	if (!isMinVersionSupported(IAP_MIN_VERSION)) return noop;
	const { options, onEvent, onError } = params;
	const sku = options.sku ?? options.productId;
	if (!getConstant("isRequestOneTimePurchaseSupported")) {
		callAsyncMethod("iapCreateOneTimePurchaseOrder", { productId: sku }).then((response) => {
			Promise.resolve(options.processProductGrant({ orderId: response.orderId })).then(() => {
				onEvent({
					type: "success",
					data: response
				});
			}).catch((error) => {
				onError(error);
			});
		}).catch((error) => {
			onError(error);
		});
		return noop;
	}
	return callEventMethod("requestOneTimePurchase", {
		params: { sku },
		onEvent: async (event) => {
			if (event.type === "purchased") {
				const isProductGranted = await options.processProductGrant({ orderId: event.data.orderId });
				await callAsyncMethod("processProductGrant", {
					orderId: event.data.orderId,
					isProductGranted
				}).catch(onError);
			} else onEvent(event);
		},
		onError: (error) => onError(error)
	});
}, {
	MIN_TOSS_APP_VERSION: IAP_MIN_VERSION,
	isSupported: () => isMinVersionSupported(IAP_MIN_VERSION)
});
//#endregion
//#region src/apis/domains/iap/createSubscriptionPurchaseOrder/createSubscriptionPurchaseOrder.ts
/**
* 구독 인앱결제 주문서 페이지로 이동해요.
* 반환되는 cleanup 함수는 결제 흐름이 끝나면 반드시 호출해야 해요.
* @since Toss - Android 5.248.0, iOS 5.249.0
*
* @deprecated 이 함수는 더 이상 사용되지 않아요. `IAP.createSubscriptionPurchaseOrder`을 사용해주세요.
*/
const createSubscriptionPurchaseOrder = Object.assign((params) => {
	const noop = () => {};
	if (!isMinVersionSupported(IAP_SUBSCRIPTION_PURCHASE_MIN_VERSION)) return noop;
	const { options, onEvent, onError } = params;
	const { sku, offerId, processProductGrant: handleProcessProductGrant } = options;
	return callEventMethod("requestSubscriptionPurchase", {
		params: {
			sku,
			offerId: offerId ?? null
		},
		onEvent: async (event) => {
			if (event.type === "purchased") {
				const isProductGranted = await handleProcessProductGrant({
					orderId: event.data.orderId,
					subscriptionId: event.data.subscriptionId
				});
				await callAsyncMethod("processProductGrant", {
					orderId: event.data.orderId,
					isProductGranted
				}).catch(onError);
			} else onEvent(event);
		},
		onError: (error) => onError(error)
	});
}, {
	MIN_TOSS_APP_VERSION: IAP_SUBSCRIPTION_PURCHASE_MIN_VERSION,
	isSupported: () => isMinVersionSupported(IAP_SUBSCRIPTION_PURCHASE_MIN_VERSION)
});
//#endregion
//#region src/apis/domains/iap/getCompletedOrRefundedOrders/getCompletedOrRefundedOrders.ts
/**
* 완료·환불된 주문 목록을 가져와요. `hasNext` / `nextKey`로 페이지네이션해요.
* @since Toss - Android 5.231.0, iOS 5.231.0
*
* @deprecated 이 함수는 더 이상 사용되지 않아요. `IAP.getCompletedOrRefundedOrders`을 사용해주세요.
*/
const getCompletedOrRefundedOrders = Object.assign(() => callAsyncMethod("getCompletedOrRefundedOrders", {}), {
	MIN_TOSS_APP_VERSION: IAP_COMPLETED_OR_REFUNDED_ORDERS_MIN_VERSION,
	isSupported: () => isMinVersionSupported(IAP_COMPLETED_OR_REFUNDED_ORDERS_MIN_VERSION)
});
//#endregion
//#region src/apis/domains/iap/getPendingOrders/getPendingOrders.ts
/**
* 아직 결제가 완료되지 않은 대기 중인 주문 목록을 가져와요.
* @since Toss - Android 5.234.0, iOS 5.231.0
*
* @deprecated 이 함수는 더 이상 사용되지 않아요. `IAP.getPendingOrders`을 사용해주세요.
*/
const getPendingOrders = Object.assign(() => callAsyncMethod("getPendingOrders"), {
	MIN_TOSS_APP_VERSION: IAP_PENDING_ORDERS_MIN_VERSION,
	isSupported: () => isMinVersionSupported(IAP_PENDING_ORDERS_MIN_VERSION)
});
//#endregion
//#region src/apis/domains/iap/getProductItemList/getProductItemList.ts
/**
* 구매 가능한 상품 목록을 가져와요.
* @since Toss - Android 5.219.0, iOS 5.219.0
*
* @deprecated 이 함수는 더 이상 사용되지 않아요. `IAP.getProductItemList`을 사용해주세요.
*/
const getProductItemList = Object.assign(() => callAsyncMethod("iapGetProductItemList"), {
	MIN_TOSS_APP_VERSION: IAP_MIN_VERSION,
	isSupported: () => isMinVersionSupported(IAP_MIN_VERSION)
});
//#endregion
//#region src/apis/domains/iap/getSubscriptionInfo/getSubscriptionInfo.ts
/**
* 구독 주문의 현재 상태 정보를 가져와요. `{ params: { orderId } }` 형태예요.
* @since Toss - Android 5.253.0, iOS 5.250.0
*
* @deprecated 이 함수는 더 이상 사용되지 않아요. `IAP.getSubscriptionInfo`을 사용해주세요.
*/
const getSubscriptionInfo = Object.assign((args) => callAsyncMethod("getSubscriptionInfo", args), {
	MIN_TOSS_APP_VERSION: IAP_SUBSCRIPTION_INFO_MIN_VERSION,
	isSupported: () => isMinVersionSupported(IAP_SUBSCRIPTION_INFO_MIN_VERSION)
});
//#endregion
//#region src/apis/domains/iap/iap.ts
const IAP = {
	/**
	* 단건 인앱결제 주문서 페이지로 이동해요.
	* 반환되는 cleanup 함수는 결제 흐름이 끝나면 반드시 호출해야 해요.
	* 상품 식별자는 `sku`를 사용하세요. (`productId`는 deprecated)
	* @since Toss - Android 5.219.0, iOS 5.219.0
	*/
	createOneTimePurchaseOrder: withUnsupportedThrow(createOneTimePurchaseOrder),
	/**
	* 구독 인앱결제 주문서 페이지로 이동해요.
	* 반환되는 cleanup 함수는 결제 흐름이 끝나면 반드시 호출해야 해요.
	* @since Toss - Android 5.248.0, iOS 5.249.0
	*/
	createSubscriptionPurchaseOrder: withUnsupportedThrow(createSubscriptionPurchaseOrder),
	/**
	* 구매 가능한 상품 목록을 가져와요.
	* @since Toss - Android 5.219.0, iOS 5.219.0
	*/
	getProductItemList: withUnsupportedThrow(getProductItemList),
	/**
	* 아직 결제가 완료되지 않은 대기 중인 주문 목록을 가져와요.
	* @since Toss - Android 5.234.0, iOS 5.231.0
	*/
	getPendingOrders: withUnsupportedThrow(getPendingOrders),
	/**
	* 완료·환불된 주문 목록을 가져와요. `hasNext` / `nextKey`로 페이지네이션해요.
	* @since Toss - Android 5.231.0, iOS 5.231.0
	*/
	getCompletedOrRefundedOrders: withUnsupportedThrow(getCompletedOrRefundedOrders),
	/**
	* 상품 지급 완료를 앱에 알려요. `{ params: { orderId } }` 형태이며, 지급 완료 여부(boolean)를 반환해요.
	* @since Toss - Android 5.233.0, iOS 5.233.0
	*/
	completeProductGrant: withUnsupportedThrow(completeProductGrant),
	/**
	* 구독 주문의 현재 상태 정보를 가져와요. `{ params: { orderId } }` 형태예요.
	* @since Toss - Android 5.253.0, iOS 5.250.0
	*/
	getSubscriptionInfo: withUnsupportedThrow(getSubscriptionInfo)
};
//#endregion
//#region src/apis/domains/migration/getOriginStorage/getOriginStorage.ts
const CALLBACK_EVENT_PREFIX = "ait:web-storage-migration:callback";
const REQUEST_TIMEOUT_MS = 6e4;
function getOriginStorage() {
	const callbackEventName = `${CALLBACK_EVENT_PREFIX}:${crypto.randomUUID()}`;
	return new Promise((resolve, reject) => {
		const cleanup = () => {
			clearTimeout(timeoutId);
			window.removeEventListener(callbackEventName, onResponse);
		};
		const rejectWith = (error) => {
			cleanup();
			reject(error instanceof Error ? error : new Error(String(error)));
		};
		const timeoutId = setTimeout(() => {
			rejectWith(/* @__PURE__ */ new Error("Web storage 요청 시간이 초과됐어요."));
		}, REQUEST_TIMEOUT_MS);
		const onResponse = (event) => {
			const response = event.detail;
			cleanup();
			if (response.type === "error") {
				reject(new Error(response.message));
				return;
			}
			resolve({
				previous: response.previous,
				current: response.current
			});
		};
		window.addEventListener(callbackEventName, onResponse);
		try {
			callAsyncMethod("requestWebStorageMigration", { callbackEventName }).catch(rejectWith);
		} catch (error) {
			rejectWith(error);
		}
	});
}
//#endregion
//#region src/apis/domains/migration/migration.ts
const Migration = { getOriginStorage };
//#endregion
//#region src/apis/domains/notification/requestAgreement/requestAgreement.ts
const MIN_TOSS_APP_VERSION$12 = {
	android: "5.255.0",
	ios: "5.255.0"
};
/**
* 알림 동의 화면을 요청하고, 동의 결과를 콜백으로 전달해요.
* `templateCode`는 콘솔 스마트발송 템플릿 코드예요. 반환되는 cleanup 함수로 콜백을 해제해야 해요.
*
* @deprecated 이 함수는 더 이상 사용되지 않아요. `Notification.requestAgreement`를 사용해주세요.
* @since Toss - Android 5.255.0, iOS 5.255.0
*/
const requestNotificationAgreement = Object.assign((params) => {
	return callEventMethod("requestNotificationAgreement", {
		params: params.options,
		onEvent: (event) => params.onEvent(event),
		onError: (error) => {
			params.onError(error);
		}
	});
}, {
	MIN_TOSS_APP_VERSION: MIN_TOSS_APP_VERSION$12,
	isSupported: () => isMinVersionSupported(MIN_TOSS_APP_VERSION$12)
});
//#endregion
//#region src/apis/domains/notification/notification.ts
const Notification = { 
/**
* 알림 동의 화면을 요청하고, 동의 결과를 콜백으로 전달해요.
* `templateCode`는 콘솔 스마트발송 템플릿 코드예요. 반환되는 cleanup 함수로 콜백을 해제해야 해요.
* @since Toss - Android 5.255.0, iOS 5.255.0
*/
requestAgreement: withUnsupportedThrow(requestNotificationAgreement) };
//#endregion
//#region src/apis/domains/permissions/getPermission/getPermission.ts
function getPermission(permission) {
	return callAsyncMethod("getPermission", permission);
}
//#endregion
//#region src/apis/domains/permissions/openPermissionDialog/openPermissionDialog.ts
function openPermissionDialog(permission) {
	return callAsyncMethod("openPermissionDialog", permission);
}
//#endregion
//#region src/apis/domains/permissions/requestPermission/requestPermission.ts
function requestPermission(permission) {
	return callAsyncMethod("requestPermission", permission);
}
//#endregion
//#region src/apis/domains/promotion/grantReward/grantReward.ts
const MIN_TOSS_APP_VERSION$11 = {
	android: "5.232.0",
	ios: "5.232.0"
};
function isSupported$2() {
	return isMinVersionSupported(MIN_TOSS_APP_VERSION$11);
}
function isGrantPromotionRewardError(error) {
	return typeof error === "object" && error !== null && "code" in error && typeof error.code === "string" && "message" in error && typeof error.message === "string";
}
/**
* 프로모션 코드를 사용해 유저에게 리워드를 지급해요.
* 성공 시 `{ key }`, 실패 시 `{ errorCode, message }` 또는 `'ERROR'`를 반환하고,
* 미지원 앱 버전이면 `undefined`를 반환해요. 실패는 reject 대신 반환값으로 흡수해요.
*
* @deprecated 이 함수는 더 이상 사용되지 않아요. `Promotion.grantReward`를 사용해주세요.
* @since Toss - Android 5.232.0, iOS 5.232.0
*/
const grantPromotionReward = Object.assign(async (params) => {
	if (!isSupported$2()) return;
	try {
		const response = await callAsyncMethod("grantPromotionReward", params);
		if (response.key) return response;
		return "ERROR";
	} catch (error) {
		if (isGrantPromotionRewardError(error)) return {
			errorCode: error.code,
			message: error.message
		};
		return "ERROR";
	}
}, {
	MIN_TOSS_APP_VERSION: MIN_TOSS_APP_VERSION$11,
	isSupported: isSupported$2
});
//#endregion
//#region src/apis/domains/promotion/openContactsInvite/openContactsInvite.ts
const MIN_TOSS_APP_VERSION$10 = {
	android: "5.223.0",
	ios: "5.223.0"
};
function isSupported$1() {
	return isMinVersionSupported(MIN_TOSS_APP_VERSION$10);
}
/**
* 친구에게 공유하고 리워드를 받을 수 있는 기능을 제공해요.
* `options.moduleId`는 콘솔 공유 리워드 UUID예요. 반환되는 cleanup 함수로 구독을 해제해야 해요.
* 미지원 앱 버전이면 아무 동작 없이 빈 cleanup 함수를 반환해요.
*
* @deprecated 이 함수는 더 이상 사용되지 않아요. `Promotion.openContactsInvite`를 사용해주세요.
* @since Toss - Android 5.223.0, iOS 5.223.0
*/
const contactsViral = Object.assign((params) => {
	if (!isSupported$1()) return () => {};
	return callEventMethod("contactsViral", {
		params: params.options,
		onEvent: params.onEvent,
		onError: (error) => params.onError(error)
	});
}, {
	MIN_TOSS_APP_VERSION: MIN_TOSS_APP_VERSION$10,
	isSupported: isSupported$1
});
//#endregion
//#region src/apis/domains/promotion/promotion.ts
function createGrantRewardError(code, message) {
	const error = new Error(message);
	error.name = code;
	error.code = code;
	return error;
}
const Promotion = {
	/**
	* 프로모션 코드를 사용해 유저에게 리워드를 지급해요. 성공 시 `{ key }`를 반환해요.
	* 지급 실패면 실패 코드(`error.code`)를 담은 에러를, 알 수 없는 오류면 `UNKNOWN_ERROR`를 throw해요.
	* @since Toss - Android 5.232.0, iOS 5.232.0
	*/
	grantReward: withUnsupportedThrow(Object.assign(async (params) => {
		const result = await grantPromotionReward({ params });
		if (result === "ERROR" || result === void 0) throw createGrantRewardError("UNKNOWN_ERROR", "리워드 지급 중 알 수 없는 오류가 발생했어요.");
		if ("errorCode" in result) throw createGrantRewardError(result.errorCode, result.message);
		return result;
	}, {
		isSupported: grantPromotionReward.isSupported,
		MIN_TOSS_APP_VERSION: grantPromotionReward.MIN_TOSS_APP_VERSION
	})),
	/**
	* 친구에게 공유하고 리워드를 받을 수 있는 기능을 제공해요.
	* `options.moduleId`는 콘솔 공유 리워드 UUID예요. 반환되는 cleanup 함수로 구독을 해제해야 해요.
	* @since Toss - Android 5.223.0, iOS 5.223.0
	*/
	openContactsInvite: withUnsupportedThrow(contactsViral)
};
/**
* 유저에게 미니앱 리뷰를 요청해요.
*
* @deprecated 이 함수는 더 이상 사용되지 않아요. `Review.request`를 사용해주세요.
* @since Toss - Android 5.253.0, iOS 5.253.0
*/
const requestReview = Object.assign(() => callAsyncMethod("requestMiniAppReview"), {
	MIN_TOSS_APP_VERSION: {
		android: "5.253.0",
		ios: "5.253.0"
	},
	isSupported: () => getConstant("isRequestReviewSupported")
});
//#endregion
//#region src/apis/domains/review/review.ts
const Review = { 
/**
* 유저에게 미니앱 리뷰를 요청해요.
* @since Toss - Android 5.253.0, iOS 5.253.0
*/
request: withUnsupportedThrow(requestReview, "isRequestReviewSupported") };
//#endregion
//#region src/apis/domains/safe-area/subscribe/subscribe.ts
/**
* Safe Area insets 변경을 구독해요. insets가 바뀔 때만 이벤트가 발생해요.
* 반환되는 구독 해제 함수는 언마운트 시 호출하세요.
*
* @deprecated 이 함수는 더 이상 사용되지 않아요. `SafeArea.subscribe`을 사용해주세요.
*/
const subscribeSafeAreaInsets = (args) => callEventMethod("safeAreaInsetsChange", {
	onEvent: args.onEvent,
	onError: () => {}
});
//#endregion
//#region src/apis/domains/safe-area/safe-area.ts
const SafeArea = {
	/**
	* 현재 Safe Area insets(top/bottom/left/right)를 동기로 반환해요.
	* 호스트가 컨텐츠 로드 전에 주입한 상수를 읽어요.
	*/
	get: () => getConstant("safeAreaInsets"),
	/**
	* Safe Area insets 변경을 구독해요. insets가 바뀔 때만 이벤트가 발생해요.
	* 반환되는 구독 해제 함수는 언마운트 시 호출하세요.
	*/
	subscribe: subscribeSafeAreaInsets
};
//#endregion
//#region src/apis/domains/safe-area/getSafeAreaInsets/getSafeAreaInsets.ts
/**
* Safe Area insets 값을 가져와요.
*
* @deprecated 이 함수는 더 이상 사용되지 않아요. `SafeArea.get`을 사용해주세요.
*/
function getSafeAreaInsets() {
	return getConstant("safeAreaInsets");
}
//#endregion
//#region src/apis/domains/safe-area/index.ts
/**
* `SafeArea`의 v2 호환 별칭이에요. 같은 인스턴스라서
* `SafeAreaInsets.get()`과 `SafeAreaInsets.subscribe()`를 그대로 쓸 수 있어요.
*/
const SafeAreaInsets = SafeArea;
//#endregion
//#region src/apis/domains/screen/close/close.ts
/**
* 현재 화면을 닫아요.
*
* @deprecated 이 함수는 더 이상 사용되지 않아요. `Screen.close`를 사용해주세요.
*/
const closeView = () => callAsyncMethod("closeView");
//#endregion
//#region src/apis/domains/screen/setAwakeMode/setAwakeMode.ts
/**
* 화면이 항상 켜져 있도록 설정하거나 해제해요. 앱 전체에 영향을 주므로 화면을 벗어날 때 이전 상태로 복구하세요.
* 앱을 벗어나면 항상 켜짐 모드가 풀릴 수 있어요.
*
* @deprecated 이 함수는 더 이상 사용되지 않아요. `Screen.setAwakeMode`를 사용해주세요.
*/
const setScreenAwakeMode = (options) => callAsyncMethod("setScreenAwakeMode", options);
//#endregion
//#region src/apis/domains/screen/setIosSwipeBack/setIosSwipeBack.ts
/**
* iOS에서 화면을 스와이프해 뒤로가기하는 기능을 켜거나 꺼요. 옵션 키는 `isEnabled`예요.
*
* @deprecated 이 함수는 더 이상 사용되지 않아요. `Screen.setIosSwipeBack`을 사용해주세요.
*/
const setIosSwipeGestureEnabled = (options) => callAsyncMethod("setIosSwipeGestureEnabled", options);
//#endregion
//#region src/apis/domains/screen/setOrientation/setOrientation.ts
const MIN_TOSS_APP_VERSION$8 = {
	android: "5.215.0",
	ios: "5.215.0"
};
/**
* 기기 화면 방향을 `'portrait'` 또는 `'landscape'`로 설정해요. 앱 전체에 영향을 주므로 화면을 벗어날 때 복구하세요.
* 앱 버전이 최소 지원 버전보다 낮으면 아무 동작도 하지 않아요.
* @since Toss - Android 5.215.0, iOS 5.215.0
*
* @deprecated 이 함수는 더 이상 사용되지 않아요. `Screen.setOrientation`을 사용해주세요.
*/
const setDeviceOrientation = Object.assign(async (options) => {
	if (!setDeviceOrientation.isSupported()) return;
	return callAsyncMethod("setDeviceOrientation", options);
}, {
	MIN_TOSS_APP_VERSION: MIN_TOSS_APP_VERSION$8,
	isSupported: () => isMinVersionSupported(MIN_TOSS_APP_VERSION$8)
});
//#endregion
//#region src/apis/domains/screen/setSecure/setSecure.ts
/**
* 화면 캡쳐를 차단하거나 허용해요. `{ enabled: true }`면 차단, `false`면 허용이에요.
*
* @deprecated 이 함수는 더 이상 사용되지 않아요. `Screen.setSecure`를 사용해주세요.
*/
const setSecureScreen = (options) => callAsyncMethod("setSecureScreen", options);
//#endregion
//#region src/apis/domains/screen/screen.ts
const Screen = {
	/**
	* 현재 화면을 닫아요.
	*/
	close: closeView,
	/**
	* 화면이 항상 켜져 있도록 설정하거나 해제해요. 앱 전체에 영향을 주므로 화면을 벗어날 때 이전 상태로 복구하세요.
	* 앱을 벗어나면 항상 켜짐 모드가 풀릴 수 있어요.
	*/
	setAwakeMode: setScreenAwakeMode,
	/**
	* 화면 캡쳐를 차단하거나 허용해요. `{ enabled: true }`면 차단, `false`면 허용이에요.
	*/
	setSecure: setSecureScreen,
	/**
	* iOS에서 화면을 스와이프해 뒤로가기하는 기능을 켜거나 꺼요. 옵션 키는 `isEnabled`예요.
	*/
	setIosSwipeBack: setIosSwipeGestureEnabled,
	/**
	* 기기 화면 방향을 `'portrait'` 또는 `'landscape'`로 설정해요. 앱 전체에 영향을 주므로 화면을 벗어날 때 복구하세요.
	* @since Toss - Android 5.215.0, iOS 5.215.0
	*/
	setOrientation: withUnsupportedThrow(setDeviceOrientation)
};
//#endregion
//#region src/apis/domains/share/createLink/createLink.ts
/** path/ogImageUrl을 네이티브에 넘기는 경로의 최소 버전이에요. 미만이면 V1 폴백을 사용해요. */
const V2_MIN_VERSION = {
	android: "5.240.0",
	ios: "5.239.0"
};
/**
* 지정한 경로를 토스 앱에서 열 수 있는 공유 링크를 만들어요.
* `path`는 `intoss://`로 시작하는 딥링크여야 해요. `ogImageUrl`은 토스앱 Android `5.240.0`, iOS `5.239.0` 이상에서만 동작해요.
*
* @deprecated 이 함수는 더 이상 사용되지 않아요. `Share.createLink`를 사용해주세요.
*/
async function getTossShareLink(path, ogImageUrl) {
	if (!isMinVersionSupported(V2_MIN_VERSION)) return await getTossShareLinkV1(path);
	const { shareLink } = await callAsyncMethod("getTossShareLink", {
		url: path,
		ogImageUrl
	});
	return shareLink;
}
async function getTossShareLinkV1(path) {
	const { shareLink } = await callAsyncMethod("getTossShareLink", {});
	const shareUrl = new URL(shareLink);
	shareUrl.searchParams.set("deep_link_value", path);
	shareUrl.searchParams.set("af_dp", path);
	return shareUrl.toString();
}
//#endregion
//#region src/apis/domains/share/sendMessage/sendMessage.ts
/**
* 네이티브 공유 시트를 열어 메시지를 공유해요.
*
* @deprecated 이 함수는 더 이상 사용되지 않아요. `Share.sendMessage`를 사용해주세요.
*/
const share = (message) => callAsyncMethod("share", message);
//#endregion
//#region src/apis/domains/share/share.ts
const Share = {
	/**
	* 지정한 경로를 토스 앱에서 열 수 있는 공유 링크를 만들어요.
	* `path`는 `intoss://`로 시작하는 딥링크여야 해요. `ogImageUrl`은 토스앱 Android `5.240.0`, iOS `5.239.0` 이상에서만 동작해요.
	*/
	createLink: ({ path, ogImageUrl }) => getTossShareLink(path, ogImageUrl),
	/**
	* 네이티브 공유 시트를 열어 메시지를 공유해요.
	*/
	sendMessage: share
};
//#endregion
//#region src/apis/domains/storage/clearItems/clearItems.ts
/**
* 로컬 저장소의 모든 값을 삭제해요.
*
* @deprecated 이 함수는 더 이상 사용되지 않아요. `Storage.clearItems`를 사용해주세요.
*/
const clearItems = () => callAsyncMethod("clearStorage");
//#endregion
//#region src/apis/domains/storage/getItem/getItem.ts
/**
* 로컬 저장소에서 문자열 값을 가져와요. 값이 없으면 `null`을 반환해요.
*
* @deprecated 이 함수는 더 이상 사용되지 않아요. `Storage.getItem`을 사용해주세요.
*/
const getItem = (key) => callAsyncMethod("getStorageItem", { key });
/**
* 로컬 저장소에서 여러 문자열 값을 한 번에 가져와요. 저장되지 않은 키의 값은 `null`이에요.
* @since Toss - Android 5.270.0, iOS 5.270.0
*/
const getItems = Object.assign((keys) => callAsyncMethod("getStorageItems", { keys }), { MIN_TOSS_APP_VERSION: {
	android: "5.270.0",
	ios: "5.270.0"
} });
//#endregion
//#region src/apis/domains/storage/removeItem/removeItem.ts
/**
* 특정 키의 값을 삭제해요.
*
* @deprecated 이 함수는 더 이상 사용되지 않아요. `Storage.removeItem`을 사용해주세요.
*/
const removeItem = (key) => callAsyncMethod("removeStorageItem", { key });
//#endregion
//#region src/apis/domains/storage/setItem/setItem.ts
/**
* 로컬 저장소에 문자열 값을 저장해요. 앱이 종료되어도 유지돼요.
*
* @deprecated 이 함수는 더 이상 사용되지 않아요. `Storage.setItem`을 사용해주세요.
*/
const setItem = (key, value) => callAsyncMethod("setStorageItem", {
	key,
	value
});
/**
* 로컬 저장소에 여러 문자열 값을 한 번에 저장해요.
* @since Toss - Android 5.270.0, iOS 5.270.0
*/
const setItems = Object.assign((items) => callAsyncMethod("setStorageItems", { items }), { MIN_TOSS_APP_VERSION: {
	android: "5.270.0",
	ios: "5.270.0"
} });
//#endregion
//#region src/apis/domains/storage/storage.ts
const Storage = {
	/**
	* 로컬 저장소에서 문자열 값을 가져와요. 값이 없으면 `null`을 반환해요.
	*/
	getItem,
	/**
	* 로컬 저장소에서 여러 문자열 값을 한 번에 가져와요. 저장되지 않은 키의 값은 `null`이에요.
	* @since Toss - Android 5.270.0, iOS 5.270.0
	*/
	getItems: withUnsupportedThrow(getItems, "isGetStorageItemsSupported"),
	/**
	* 로컬 저장소에 문자열 값을 저장해요. 앱이 종료되어도 유지돼요.
	*/
	setItem,
	/**
	* 로컬 저장소에 여러 문자열 값을 한 번에 저장해요.
	* @since Toss - Android 5.270.0, iOS 5.270.0
	*/
	setItems: withUnsupportedThrow(setItems, "isSetStorageItemsSupported"),
	/**
	* 특정 키의 값을 삭제해요.
	*/
	removeItem,
	/**
	* 로컬 저장소의 모든 값을 삭제해요.
	*/
	clearItems
};
//#endregion
//#region src/apis/domains/toss-auth/isIntegrated/isIntegrated.ts
const MIN_TOSS_APP_VERSION$5 = {
	android: "5.237.0",
	ios: "5.237.0"
};
/**
* 유저가 토스 로그인을 연동했는지 여부를 확인해요.
* 토스 로그인을 쓰지 않는 미니앱에서 호출하면 설정 오류로 throw될 수 있어요.
* 앱 버전이 최소 지원 버전보다 낮으면 아무 동작도 하지 않고 `undefined`를 반환해요.
* @since Toss - Android 5.237.0, iOS 5.237.0
*
* @deprecated 이 함수는 더 이상 사용되지 않아요. `TossAuth.isIntegrated`를 사용해주세요.
*/
const getIsTossLoginIntegratedService = Object.assign(async () => {
	if (!getIsTossLoginIntegratedService.isSupported()) return;
	return callAsyncMethod("getIsTossLoginIntegratedService");
}, {
	MIN_TOSS_APP_VERSION: MIN_TOSS_APP_VERSION$5,
	isSupported: () => isMinVersionSupported(MIN_TOSS_APP_VERSION$5)
});
//#endregion
//#region src/apis/domains/toss-auth/login/login.ts
/**
* 토스 인증으로 로그인해요. 로그인이 완료되면 다시 토스 앱으로 이동해요.
* 반환된 `authorizationCode`와 `referrer`는 서버로 전달해야 해요.
*
* @deprecated 이 함수는 더 이상 사용되지 않아요. `TossAuth.login`을 사용해주세요.
*/
const appLogin = () => callAsyncMethod("appLogin");
//#endregion
//#region src/apis/domains/toss-auth/sign/sign.ts
const MIN_TOSS_APP_VERSION$4 = {
	android: "5.233.0",
	ios: "5.233.0"
};
/**
* 토스 인증서를 사용해 서명해요. 본인확인·간편인증·전자서명의 `txId`가 필요해요.
* 앱 버전이 최소 지원 버전보다 낮으면 경고 로그만 남기고 아무 동작도 하지 않아요.
* @since Toss - Android 5.233.0, iOS 5.233.0
*
* @deprecated 이 함수는 더 이상 사용되지 않아요. `TossAuth.sign`을 사용해주세요.
*/
const appsInTossSignTossCert = Object.assign(async (params) => {
	if (!appsInTossSignTossCert.isSupported()) {
		console.warn("appsInTossSignTossCert is not supported in this app version");
		return;
	}
	await callAsyncMethod("appsInTossSignTossCert", params);
}, {
	MIN_TOSS_APP_VERSION: MIN_TOSS_APP_VERSION$4,
	isSupported: () => isMinVersionSupported(MIN_TOSS_APP_VERSION$4)
});
//#endregion
//#region src/apis/domains/toss-auth/toss-auth.ts
const TossAuth = {
	/**
	* 토스 인증으로 로그인해요. 로그인이 완료되면 다시 토스 앱으로 이동해요.
	* 반환된 `authorizationCode`와 `referrer`는 서버로 전달해야 해요.
	*/
	login: appLogin,
	/**
	* 유저가 토스 로그인을 연동했는지 여부를 확인해요.
	* 토스 로그인을 쓰지 않는 미니앱에서 호출하면 설정 오류로 throw될 수 있어요.
	* @since Toss - Android 5.237.0, iOS 5.237.0
	*/
	isIntegrated: withUnsupportedThrow(getIsTossLoginIntegratedService),
	/**
	* 토스 인증서를 사용해 서명해요. 본인확인·간편인증·전자서명의 `txId`가 필요해요.
	* @since Toss - Android 5.233.0, iOS 5.233.0
	*/
	sign: withUnsupportedThrow(appsInTossSignTossCert)
};
//#endregion
//#region src/apis/domains/toss-pay/authorize/authorize.ts
/**
* 토스페이 결제창을 띄우고 사용자 인증을 수행해요.
* 결제창을 통해 사용자 인증만 하며, 실제 결제 처리는 인증 성공 후 서버에서 별도로 해야 해요.
* `payToken`이 필요하며 `{ success, reason? }`를 반환해요.
*
* @deprecated 이 함수는 더 이상 사용되지 않아요. `TossPay.authorize`를 사용해주세요.
*/
function checkoutPayment(options) {
	return callAsyncMethod("checkoutPayment", options.params);
}
//#endregion
//#region src/apis/domains/toss-pay/authorizeSubscription/authorizeSubscription.ts
const MIN_TOSS_APP_VERSION$3 = {
	android: "5.256.0",
	ios: "5.256.0"
};
function isSupported() {
	return getConstant("isRequestTossPayPaysBillingSupported");
}
/**
* 와이어 호출부예요. 도메인 멤버(`TossPay.authorizeSubscription`)가 flat 인자를
* 감싸서 사용하고, 함수형 API(`requestTossPayPaysBilling`)는 여기에 v2의
* 경고-후-undefined 경로를 얹어요.
*/
function callRequestTossPayPaysBilling(options) {
	return callAsyncMethod("requestTossPayPaysBilling", options.params);
}
/**
* 토스페이 정기결제창을 띄우고 사용자 인증을 수행해요.
* 결제창을 통해 사용자 인증만 하며, 실제 결제 처리는 인증 성공 후 서버에서 별도로 해야 해요.
* 미지원 버전이면 경고 로그 후 `undefined`를 반환해요. `isSupported()`로 지원 여부를 확인할 수 있어요.
* @since Toss - Android 5.256.0, iOS 5.256.0
*
* @deprecated 이 함수는 더 이상 사용되지 않아요. `TossPay.authorizeSubscription`을 사용해주세요.
*/
const requestTossPayPaysBilling = Object.assign(async (options) => {
	if (!isSupported()) {
		console.warn("requestTossPayPaysBilling is not supported in this app version");
		return;
	}
	return callRequestTossPayPaysBilling(options);
}, {
	MIN_TOSS_APP_VERSION: MIN_TOSS_APP_VERSION$3,
	isSupported
});
//#endregion
//#region src/apis/domains/toss-pay/toss-pay.ts
const TossPay = {
	/**
	* 토스페이 결제창을 띄우고 사용자 인증을 수행해요.
	* 결제창을 통해 사용자 인증만 하며, 실제 결제 처리는 인증 성공 후 서버에서 별도로 해야 해요.
	* `payToken`이 필요하며 `{ success, reason? }`를 반환해요.
	*/
	authorize: (params) => checkoutPayment({ params }),
	/**
	* 토스페이 정기결제창을 띄우고 사용자 인증을 수행해요.
	* 결제창을 통해 사용자 인증만 하며, 실제 결제 처리는 인증 성공 후 서버에서 별도로 해야 해요.
	* `wrappedToken`이 필요하며 `{ success, reason? }`를 반환해요.
	* @since Toss - Android 5.256.0, iOS 5.256.0
	*/
	authorizeSubscription: withUnsupportedThrow(Object.assign(async (params) => {
		return await callRequestTossPayPaysBilling({ params });
	}, { MIN_TOSS_APP_VERSION: requestTossPayPaysBilling.MIN_TOSS_APP_VERSION }), "isRequestTossPayPaysBillingSupported"),
	/**
	* @deprecated TossPay.authorize를 사용해주세요
	*/
	checkoutPayment,
	/**
	* @deprecated TossPay.authorizeSubscription을 사용해주세요
	*/
	requestTossPayPaysBilling
};
//#endregion
//#region src/apis/domains/user/getAnonymousKey/getAnonymousKey.ts
const MIN_TOSS_APP_VERSION$2 = {
	android: "5.232.0",
	ios: "5.232.0"
};
/**
* 미니앱에서 사용자의 고유 키를 가져와요. 성공 시 `{ type: 'HASH', hash }`를 반환해요.
* 알 수 없는 오류가 발생하면 `'ERROR'`를, 앱 버전이 최소 지원 버전보다 낮으면 `undefined`를 반환해요.
* @since Toss - Android 5.232.0, iOS 5.232.0
*
* @deprecated 이 함수는 더 이상 사용되지 않아요. `User.getAnonymousKey`을 사용해주세요.
*/
const getAnonymousKey = Object.assign(async () => {
	if (!getAnonymousKey.isSupported()) return;
	try {
		const response = await callAsyncMethod("getAnonymousKey");
		if (response.type === "HASH") return response;
		return "ERROR";
	} catch {
		return "ERROR";
	}
}, {
	MIN_TOSS_APP_VERSION: MIN_TOSS_APP_VERSION$2,
	isSupported: () => isMinVersionSupported(MIN_TOSS_APP_VERSION$2)
});
/**
* 사용자 동의 기반 데이터를 요청해요. `consentedUserDataKey`가 필요하고, 동의가 필요하면 약관 웹뷰를 띄워요.
* 미지원 토스 앱 버전에서는 undefined를 반환해요.
* @since Toss - Android 5.264.0, iOS 5.264.0
*
* @deprecated 이 함수는 더 이상 사용되지 않아요. `User.getConsentedData`을 사용해주세요.
*/
const getConsentedUserData = Object.assign(async (options) => {
	if (!getConsentedUserData.isSupported()) return;
	return await callAsyncMethod("getConsentedUserData", options);
}, {
	MIN_TOSS_APP_VERSION: {
		android: "5.264.0",
		ios: "5.264.0"
	},
	isSupported: () => getConstant("isGetConsentedUserDataSupported")
});
/**
* iOS에서 선언된 연령 범위를 조회해요. iOS 26 이상과 토스앱 5.266.0 이상이 필요하고, `{ ageGates: number[] }`를 넘기세요.
* `isSupported()`로 지원 여부를 확인할 수 있어요.
* @since Toss - iOS 5.266.0, OS - iOS 26.0
*
* @deprecated 이 함수는 더 이상 사용되지 않아요. `User.getDeclaredAgeRange`을 사용해주세요.
*/
const getDeclaredAgeRange = Object.assign(async (params) => {
	return await callAsyncMethod("getDeclaredAgeRange", params);
}, {
	MIN_TOSS_APP_VERSION: { ios: "5.266.0" },
	MIN_OS_VERSION: { ios: "26.0" },
	isSupported: () => getConstant("isGetDeclaredAgeRangeSupported")
});
//#endregion
//#region src/apis/domains/user/user.ts
function createUnknownError(message) {
	const error = new Error(message);
	error.name = "UNKNOWN_ERROR";
	error.code = "UNKNOWN_ERROR";
	return error;
}
const User = {
	/**
	* 미니앱에서 사용자의 고유 키를 가져와요. 성공 시 `{ type: 'HASH', hash }`를 반환해요.
	* 앱 버전이 부족하면 `UNSUPPORTED_APP_VERSION`, 알 수 없는 오류면 `UNKNOWN_ERROR`를 throw해요.
	* @since Toss - Android 5.232.0, iOS 5.232.0
	*/
	getAnonymousKey: withUnsupportedThrow(Object.assign(async () => {
		const result = await getAnonymousKey();
		if (result === "ERROR") throw createUnknownError("사용자 키 조회 중 알 수 없는 오류가 발생했어요.");
		return result;
	}, {
		isSupported: getAnonymousKey.isSupported,
		MIN_TOSS_APP_VERSION: getAnonymousKey.MIN_TOSS_APP_VERSION
	})),
	/**
	* 사용자 동의 기반 데이터를 요청해요. `consentedUserDataKey`가 필요하고, 동의가 필요하면 약관 웹뷰를 띄워요.
	* @since Toss - Android 5.264.0, iOS 5.264.0
	*/
	getConsentedData: withUnsupportedThrow(getConsentedUserData, "isGetConsentedUserDataSupported"),
	/**
	* iOS에서 선언된 연령 범위를 조회해요. iOS 26 이상과 토스앱 5.266.0 이상이 필요하고, `{ ageGates: number[] }`를 넘기세요.
	* OS 버전이 부족하면 `UNSUPPORTED_OS_VERSION`, 토스앱 버전이 부족하면 `UNSUPPORTED_APP_VERSION` 에러를 던져요.
	* @since Toss - iOS 5.266.0, OS - iOS 26.0
	*/
	getDeclaredAgeRange: withUnsupportedThrow(getDeclaredAgeRange, "isGetDeclaredAgeRangeSupported")
};
//#endregion
//#region src/apis/domains/_viral/getCampaign/getCampaign.ts
/**
* 바이럴 캠페인 정보를 조회해요.
*/
const getCampaign = (options) => callAsyncMethod("getViralCampaign", options);
//#endregion
//#region src/apis/domains/_viral/share/share.ts
/**
* 바이럴 캠페인을 카카오톡으로 공유해요.
*/
const share$1 = (options) => callAsyncMethod("shareViralCampaign", options);
//#endregion
//#region src/apis/domains/_viral/viral.ts
const Viral = {
	/**
	* 바이럴 캠페인 정보를 조회해요.
	*/
	getCampaign,
	/**
	* 바이럴 캠페인을 카카오톡으로 공유해요.
	*/
	share: share$1
};
//#endregion
//#region src/apis/events/createEventEmitter.ts
/**
* 계약 이벤트 메서드 위에 도메인별 이벤트 맵을 얹는 헬퍼예요.
* 이벤트 이름·핸들러·옵션 타입 모두 계약(EventMethodsMap)으로 검증돼요.
*/
function createEventEmitter() {
	return { addEventListener: (event, args) => {
		return callEventMethod(event, {
			params: args.options,
			onEvent: args.onEvent,
			onError: args.onError ?? (() => {})
		});
	} };
}
//#endregion
//#region src/apis/events/appsInTossEvent/appsInTossEvent.ts
/**
* 앱인토스 전용 이벤트 버스예요. 현재 등록된 이벤트는 없어요.
*/
const appsInTossEvent = createEventEmitter();
//#endregion
//#region src/apis/events/graniteEvent/graniteEvent.ts
/**
* 시스템 뒤로가기(`backEvent`)·홈 이동(`homeEvent`) 이벤트를 구독해요.
* 반환되는 함수를 호출하면 구독이 해제돼요.
*/
const graniteEvent = createEventEmitter();
//#endregion
//#region src/apis/events/_onVisibilityChangedByTransparentServiceWeb/onVisibilityChangedByTransparentServiceWeb.ts
/**
* 투명 서비스 웹뷰의 가시성 변화를 구독해요. callbackId로 웹뷰를 구분하고,
* cleanup 함수를 반환해요.
*/
const onVisibilityChangedByTransparentServiceWeb = (eventParams) => callEventMethod("onVisibilityChangedByTransparentServiceWeb", {
	params: eventParams.options,
	onEvent: eventParams.onEvent,
	onError: eventParams.onError
});
//#endregion
//#region src/apis/events/_tdsEvent/tdsEvent.ts
/**
* TDS 네비게이션 바 이벤트(`navigationAccessoryEvent`)를 구독해요.
* 반환되는 함수를 호출하면 구독이 해제돼요.
*/
const tdsEvent = createEventEmitter();
//#endregion
//#region src/apis/_partner/addAccessoryButton/addAccessoryButton.ts
/**
* 네비게이션 바에 액세서리 버튼을 추가해요.
* 버튼이 눌리면 `tdsEvent.navigationAccessoryEvent`로 `{ id }`가 전달돼요.
*/
function addAccessoryButton(params) {
	return callAsyncMethod("addAccessoryButton", params);
}
//#endregion
//#region src/apis/_partner/removeAccessoryButton/removeAccessoryButton.ts
/**
* `addAccessoryButton`으로 추가한 액세서리 버튼을 제거해요.
*/
function removeAccessoryButton() {
	return callAsyncMethod("removeAccessoryButton");
}
//#endregion
//#region src/apis/_partner/index.ts
/**
* 파트너 웹뷰 전용 네비게이션 바 제어예요.
* (flat 함수는 SDK 표면에 없어서 노출하지 않아요 — partner 객체로만 써요)
*/
const partner = {
	addAccessoryButton,
	removeAccessoryButton
};
//#endregion
//#region src/v2-compat.ts
/**
* @deprecated v2 호환용 별칭. `getAnonymousKey` 또는 `User.getAnonymousKey`를 사용해주세요.
*/
const getUserKeyForGame = getAnonymousKey;
/**
* @deprecated v2 호환용 별칭. `grantPromotionReward` 또는 `Promotion.grantReward`를 사용해주세요.
*/
const grantPromotionRewardForGame = grantPromotionReward;
//#endregion
exports.Accuracy = Accuracy;
exports.Analytics = Analytics;
exports.Clipboard = Clipboard;
exports.Device = Device;
exports.Environment = Environment;
exports.FetchAlbumPhotosPermissionError = FetchAlbumPhotosPermissionError;
exports.FetchContactsPermissionError = FetchContactsPermissionError;
exports.File = File;
exports.Game = Game;
exports.GetClipboardTextPermissionError = GetClipboardTextPermissionError;
exports.GetCurrentLocationPermissionError = GetCurrentLocationPermissionError;
exports.GoogleAdMob = GoogleAdMob;
exports.IAP = IAP;
exports.Migration = Migration;
exports.Notification = Notification;
exports.OpenCameraPermissionError = OpenCameraPermissionError;
exports.PermissionError = PermissionError;
exports.Promotion = Promotion;
exports.Review = Review;
exports.SafeArea = SafeArea;
exports.SafeAreaInsets = SafeAreaInsets;
exports.Screen = Screen;
exports.SetClipboardTextPermissionError = SetClipboardTextPermissionError;
exports.Share = Share;
exports.StartUpdateLocationPermissionError = StartUpdateLocationPermissionError;
exports.Storage = Storage;
exports.TossAds = TossAds;
exports.TossAuth = TossAuth;
exports.TossPay = TossPay;
exports.User = User;
exports.Viral = Viral;
exports.appLogin = appLogin;
exports.appsInTossEvent = appsInTossEvent;
exports.appsInTossSignTossCert = appsInTossSignTossCert;
exports.checkoutPayment = checkoutPayment;
exports.closeView = closeView;
exports.contactsViral = contactsViral;
exports.createAsyncBridge = createAsyncBridge;
exports.createConstantBridge = createConstantBridge;
exports.createEventBridge = createEventBridge;
exports.env = env;
exports.eventLog = eventLog;
exports.fetchAlbumItems = fetchAlbumItems;
exports.fetchAlbumPhotos = fetchAlbumPhotos;
exports.fetchContacts = fetchContacts;
exports.generateHapticFeedback = generateHapticFeedback;
exports.getAnonymousKey = getAnonymousKey;
exports.getAppsInTossGlobals = getAppsInTossGlobals;
exports.getClipboardText = getClipboardText;
exports.getConsentedUserData = getConsentedUserData;
exports.getCurrentLocation = getCurrentLocation;
exports.getDeclaredAgeRange = getDeclaredAgeRange;
exports.getDeviceId = getDeviceId;
exports.getGameCenterGameProfile = getGameCenterGameProfile;
exports.getGroupId = getGroupId;
exports.getIsTossLoginIntegratedService = getIsTossLoginIntegratedService;
exports.getLocale = getLocale;
exports.getNetworkStatus = getNetworkStatus;
exports.getOperationalEnvironment = getOperationalEnvironment;
exports.getPermission = getPermission;
exports.getPlatformOS = getPlatformOS;
exports.getSafeAreaInsets = getSafeAreaInsets;
exports.getSchemeUri = getSchemeUri;
exports.getServerTime = getServerTime;
exports.getTossAppVersion = getTossAppVersion;
exports.getTossShareLink = getTossShareLink;
exports.getUserKeyForGame = getUserKeyForGame;
exports.graniteEvent = graniteEvent;
exports.grantPromotionReward = grantPromotionReward;
exports.grantPromotionRewardForGame = grantPromotionRewardForGame;
exports.isMinVersionSupported = isMinVersionSupported;
exports.loadFullScreenAd = loadFullScreenAd;
exports.onVisibilityChangedByTransparentServiceWeb = onVisibilityChangedByTransparentServiceWeb;
exports.openCamera = openCamera;
exports.openGameCenterLeaderboard = openGameCenterLeaderboard;
exports.openPDFViewer = openPDFViewer;
exports.openPermissionDialog = openPermissionDialog;
exports.openURL = openURL;
exports.partner = partner;
exports.requestNotificationAgreement = requestNotificationAgreement;
exports.requestPermission = requestPermission;
exports.requestReview = requestReview;
exports.requestTossPayPaysBilling = requestTossPayPaysBilling;
exports.saveBase64Data = saveBase64Data;
exports.setClipboardText = setClipboardText;
exports.setDeviceOrientation = setDeviceOrientation;
exports.setIosSwipeGestureEnabled = setIosSwipeGestureEnabled;
exports.setScreenAwakeMode = setScreenAwakeMode;
exports.setSecureScreen = setSecureScreen;
exports.share = share;
exports.showFullScreenAd = showFullScreenAd;
exports.startUpdateLocation = startUpdateLocation;
exports.submitGameCenterLeaderBoardScore = submitGameCenterLeaderBoardScore;
exports.tdsEvent = tdsEvent;
